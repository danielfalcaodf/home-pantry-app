# Ordem de implementação — changes ativas

Registro central da ORDEM cronológica recomendada de `/opsx:apply` entre as changes **ativas**
deste repositório (não arquivadas). Não é um mecanismo do OpenSpec CLI — é convenção deste
projeto, mantida por `/opsx:propose`, `/opsx:update` e `/opsx:archive`, porque não há
isolamento por branch entre changes: duas em andamento ao mesmo tempo podem tocar o mesmo
arquivo (mesma feature em `src/domain|application|infrastructure|presentation`, mesmo
`src/infrastructure/db/schema.ts`/migrations, mesma rota em `app/`) sem que nada avise.

Regras:

- Todos os comandos `opsx` operam sempre sobre as changes **atuais** (ativas) deste registro —
  nunca sobre changes arquivadas.
- Uma change sai desta lista quando é arquivada. Change sem dependência conhecida entra com
  "—" em "Depende de" — ainda assim ocupa uma posição, para deixar claro que não há ordem
  forçada com o que já está na lista.
- A coluna **Ordem** também define o nome da branch do PR criado no arquivamento:
  `feature/<NN>-<nome-da-change>` ou `fix/<NN>-<nome-da-change>` (prefixo conforme o
  `**Type:**` do `proposal.md` da change — Nova Feature → `feature/`, Correção de Bug →
  `fix/`; NN = Ordem com dois dígitos). A branch do PR respeita, obrigatoriamente, a ordem
  cronológica das changes. Título do PR segue a mesma convenção de commit do projeto:
  `feat: <descrição>` / `fix: <descrição>` (ver CLAUDE.md, seção "Fluxo Git").
- Uma change só pode ser arquivada (e virar PR) quando 100% do fluxo de testes de `/opsx:test`
  passa (TDD, QA e testes de contexto do bug, conforme o tipo).

Changes 3 a 13 foram implementadas (`/opsx:apply`) em sessão dedicada só a código, sem rodar o
fluxo de testes (`/opsx:test`) — testes ficam para sessão separada, por change, antes de cada
`/opsx:archive`. Cada change tem sua própria branch curta (`fix/NN-<nome>`), criada a partir de
`develop`, respeitando a coluna "Depende de" desta tabela.

| Ordem | Change | Depende de | Motivo | Testes |
|---|---|---|---|---|
| 1 | correcao-usabilidade-campos-e-botoes | — | Corrige Keyboard Overlap, Error Prevention (correção silenciosa) e Affordance na base compartilhada (CampoTexto, Botao, padrão de Modal dos sheets) — sem dependência de outra change ativa. | Já em `develop` (mergeada antes desta sessão). |
| 2 | correcao-lista-de-compras | 1 | Reaproveita CampoTexto/EvitaTeclado/ícones de affordance entregues pela change 1 (já mergeada em develop) para corrigir scroll ausente, reatividade da Lista de compras, affordance do ajuste de preço no Modo Compra, e adicionar edição de preço direto na Lista. Sem sobreposição de arquivos com a change 1. | Já em `develop` (mergeada antes desta sessão). |
| 3 | correcao-bordas-do-sistema | — | Achados A-04 e A-14 da auditoria de UI/UX. `react-native-safe-area-context` e `expo-status-bar` estão instaladas e nunca foram importadas: conteúdo invade a barra de status e a faixa de gestos em toda tela, e no tema Porcelana o relógio e os ícones do sistema ficam ilegíveis. Causa raiz única com alcance global, e sem sobreposição de arquivos com as changes 1 e 2 (toca `app/_layout.tsx`, `app/(tabs)/_layout.tsx`, wrappers de tela e o provider de tema) — por isso vai primeiro do lote de auditoria. | Pendente — `/opsx:test` não executado nesta sessão. |
| 4 | correcao-teclado-em-sheets | 1, 3 | Achados A-06 (grave) e A-07 da auditoria. O teclado cobre por inteiro os 5 painéis inferiores: `<Modal>` do RN abre janela nativa separada e o `KeyboardProvider` da raiz não a alcança, então o `EvitaTeclado` entregue pela change 1 nunca recebe evento. Reabre parte do escopo do Keyboard Overlap da change 1 (já em `develop`) — é gap de correção, não causa nova. Atinge o KPI K4 via `teclado-quantidade`. Sem sobreposição de arquivos com a change 2; passa a depender também da 3 porque a correção da hipótese D (`statusBarTranslucent`/`navigationBarTranslucent`, achada por pesquisa em context7) toca o mesmo bloco de `app/_layout.tsx` onde a 3 monta `<StatusBar>` e o consumo de insets. | Pendente — `/opsx:test` não executado nesta sessão. |
| 5 | correcao-acoes-fora-de-alcance | 3 | Achados A-09, A-10 e A-13 da auditoria (A-09 e A-10 relatados pelo usuário). Três ações escritas como texto clicável no fluxo do conteúdo: CTA do formulário que sai da dobra ao expandir "Mais opções" ou abrir o teclado, "Adicionar item avulso" como rodapé depois do 41º item e com alvo de ~20dp, e "Ver histórico" do Resumo com 22dp. Depende da 3 porque ancorar a ação no rodapé exige o inset inferior — sem ele a ação nasce dentro da faixa de gestos. | Pendente — `/opsx:test` não executado nesta sessão. |
| 6 | correcao-affordance-busca-e-lista | 5 | Achados A-01, A-02, A-03 e A-05 da auditoria. Affordance e estado que a change 1 deu à base de componentes nunca chegaram à tela de busca nem ao cabeçalho da Lista: o toggle "Agrupar" renderiza idêntico ligado e desligado (zero canal visual de estado), os chips mantêm a contagem total durante busca sem resultado, o estado vazio da busca é centralizado ignorando o teclado, e o campo de busca sublinha nome de produto como erro ortográfico. Depende da 5 por sobreposição em `app/(tabs)/lista.tsx` — a 5 reorganiza o cabeçalho onde este toggle vive. | Pendente — `/opsx:test` não executado nesta sessão. |
| 7 | correcao-agrupamento-modo-compra | 2 | Achado A-08 da auditoria. O Modo Compra preserva a ordenação por categoria vinda da Lista mas não renderiza os cabeçalhos: a ordem parece aleatória justamente na tela usada andando pelos corredores do mercado. Reaproveita `agrupar-lista.ts`, que já existe e já é testado — zero lógica nova. Parte do código de Modo Compra entregue pela change 2 (já em `develop`). | Pendente — `/opsx:test` não executado nesta sessão. |
| 8 | correcao-nomes-e-estados-acessiveis | 7 | Achados A-11, A-12 e A-15 da auditoria. O `accessibilityLabel`/`Role`/`State` do checkbox do Modo Compra vive num `View` interno e o RN colapsa a subárvore, então o leitor de tela não recebe papel nem estado — só o glifo `✓` colado no nome sintetizado. Mais prosa em CAIXA ALTA pelo papel `caption` em Configurações (inclusive o aviso de ação destrutiva) e dois nós "Despensa" na mesma tela. Depende da 7: mesmo `item-compra.tsx` e mesmo flow de regressão do Modo Compra. | Pendente — `/opsx:test` não executado nesta sessão. |
| 9 | correcao-item-excluido-reaparece-no-modo-compra | — | Achado A-16, encontrado por leitura de código a partir de relato de usuário: remover um item faltante da lista ("Fora da lista por agora") e iniciar a compra em seguida faz o item reaparecer como linha marcável no Modo Compra. `use-modo-compra.ts:52` lê `compra_item` sem filtrar `excluido` — o único dos três consumidores da tabela que não filtra (`use-lista-compras.ts` e `use-iniciar-compra.ts` já filtram certo). Correção de uma linha, sem sobreposição de arquivo com nenhuma change ativa. | Pendente — `/opsx:test` não executado nesta sessão. |
| 10 | correcao-reciclagem-de-lista-anima-item-errado | 6 | Achado A-20, relatado por usuária de teste ("duas barras verdes", animação cortando texto) e confirmado por causa raiz única: a `FlashList` da Despensa recicla views ao rolar, mas `MedidorNivel` guarda a altura num `useSharedValue` sem reset por id do item — a mola do gesto de consumir dispara para célula reciclada mostrando item diferente. A documentação oficial do `@shopify/flash-list` (guia "React Native Reanimated") prescreve exatamente essa correção (`useRecyclingState` ou reset por id). Depende da 6 por sobreposição em `app/(tabs)/index.tsx` (regiões diferentes do arquivo). | Pendente — `/opsx:test` não executado nesta sessão. |
| 11 | correcao-acabamento-header-stepper-e-affordance | 3, 8 | Quatro achados pequenos de causas independentes (A-17 a A-22, exceto A-19 e A-20 já registrados acima) de uma varredura de feedback de usuário: respiro insuficiente do cabeçalho mesmo após o inset do sistema (`espaco.xs` no wrapper do `BotaoVoltar`), assimetria de peso visual entre os botões de consumir/repor (contorno vs. preenchido), botão "Outra quantidade" quebrando alinhamento com fonte ampliada, e ajuste de preço/quantidade no Modo Compra só por toque longo sem affordance visível (mesmo padrão já resolvido em `produto/[id].tsx`, nunca replicado em `item-compra.tsx`). Depende da 3 (mesmo bloco de cabeçalho/inset) e da 8 (mesmo `item-compra.tsx`). | Pendente — `/opsx:test` não executado nesta sessão. |
| 12 | correcao-grafico-cobre-ver-historico | — | Achado A-23, achado na auditoria de 16/08 (observações #2509/#2510) mas nunca consolidado em nenhuma das changes acima. Na tela Resumo, o `GraficoBarras` não reserva clearance própria: quando um mês concentra o gasto (fração de barra próxima de 1), a barra cobre visualmente o botão "Ver histórico" no cabeçalho acima — confirmado ao vivo no emulador fechando uma compra de R$ 100,00. Causa raiz isolada em `grafico-barras.tsx`/`resumo.tsx`, sem sobreposição de arquivo com nenhuma change ativa. | Pendente — `/opsx:test` não executado nesta sessão. |
| 13 | correcao-chip-invisivel-em-sheet | — | Achado A-24, mesma auditoria de 16/08 (observações #2521/#2526), queixa original do usuário ("alguns modais que botão de mult escolhas não selecionada nenhum") nunca resolvida — a investigação da época concluiu erroneamente que a lógica "parecia correta". Causa raiz: `ChipEstado` usa `tema.bg.raised` como fundo do estado ativo quando nenhuma `cor` é passada, e é exatamente a cor de fundo de todo `<Modal>`/bottom sheet do app — o chip ativo fica visualmente idêntico ao fundo do próprio modal. Confirmado ao vivo em dois sheets (`sheet-avulso.tsx` "Medida", `sheet-ajuste-estoque.tsx` "Motivo"). Corrige só `chip-estado.tsx` (causa raiz compartilhada); sem sobreposição de arquivo com nenhuma change ativa, inclusive a 12. | Pendente — `/opsx:test` não executado nesta sessão. |
