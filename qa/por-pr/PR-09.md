---
pr: 9
branch: feature/backup-restore-json
commit: 5b50cf6
estado: concluida
---

## Foco do checklist

Restauração transacional, **idempotência** da restauração repetida, versão de schema mais nova recusada, arquivo malformado recusado antes de escrever, reconciliação. Nota do plano-fonte: `backup-pre-migration.ts` sem teste — confirmar se persiste.

## `npm run verificar`

Verde, exit 0, sem warnings.

## `npm test`

```
Test Suites: 49 passed, 49 total
Tests:       470 passed, 470 total
Time:        8.59 s
```

## Verificações específicas

- **Restauração transacional**: `sqlite-backup.repository.test.ts` — "falha no meio da transação não deixa estado parcial"; "duas compras abertas com ids diferentes (local e do backup) rejeitam a transação inteira".
- **Idempotência**: `sqlite-backup.repository.test.ts:278` — "restauração repetida é idempotente — mesmo arquivo duas vezes não duplica nada". **Confirmado, presente e coberto** (o plano-fonte listava isso como item a verificar; está testado).
- **Versão mais nova recusada**: `use-restaurar-backup.test.ts:75` — "backup de versão mais nova é recusado com o texto de FRONTEND §11".
- **Arquivo malformado recusado antes de escrever**: `use-restaurar-backup.test.ts:40` — "arquivo malformado é recusado antes de qualquer escrita"; `:58` — "texto que não é JSON válido é recusado com a mesma mensagem".
- **Reconciliação**: `sqlite-backup.repository.test.ts` — "backup íntegro restaurado não deixa nenhuma divergência"; "backup adulterado (quantidade não bate com a soma dos movimentos) é detectado após restaurar"; `use-restaurar-backup.test.ts:154` — "confirmar aplica a restauração e roda a reconciliação ao final".
- **Reescrita de casaId (design D8)**: `sqlite-backup.repository.test.ts` — "insere tudo sob a casa local, reescrevendo o casaId"; "nunca insere uma segunda casa — atualiza o nome da casa local"; "combina com dados criados no aparelho depois do backup, sem apagar nenhum".
- **Falha na restauração sem estado parcial visível**: `use-restaurar-backup.test.ts:179`.

## Verificação da nota do plano-fonte

`src/infrastructure/db/backup-pre-migration.ts` **continua sem nenhum arquivo de teste** nesta PR — confirma [[ACHADO-004]] (já registrado na origem real do arquivo, PR 3). Não abre achado duplicado.

## Achados

Nenhum novo (ACHADO-004 já cobre a lacuna confirmada aqui).

**PR-09 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-03-backup-restore-json`. 3 capabilities, ~35 cenários mapeados.

### `exportacao-de-backup` (11 cenários)

| Cenário | Teste | Status |
|---|---|---|
| JSON contém versão+data+casa+usuários+produtos+movimentos+compras+itens | `sqlite-backup.repository.test.ts:85` | Coberto |
| Versão de schema declarada | `sqlite-backup.repository.test.ts:85` | Coberto |
| Formato JSON legível, não cópia binária | `use-exportar-backup.test.ts:19` | Coberto |
| Valores em unidades internas, sem conversão | `use-exportar-backup.test.ts:36` | Coberto |
| Produtos removidos logicamente incluídos | `sqlite-backup.repository.test.ts:99` | Coberto |
| Histórico completo sem truncamento | `sqlite-backup.repository.test.ts:113` | Coberto |
| Compras abertas e finalizadas incluídas | `sqlite-backup.repository.test.ts:136` | Coberto |
| Compartilhamento via share sheet do sistema | implementação `expo-sistema-de-arquivos.ts:8-22`; adaptador real sem teste (só dublê) — **ACHADO-040** | Gap |
| Nome de arquivo com data | `use-exportar-backup.test.ts:31` | Coberto |
| Exportação funciona offline | garantido por ausência estrutural de I/O de rede; sem teste nomeado | Gap (implícito) |
| Exportação CSV separada, formatada, rotulada como dado (não backup) | `exportar-csv.test.ts:28`; `use-exportar-dados.test.ts:17`; rótulo em `configuracoes.tsx:120-129` | Coberto |

### `restauracao-de-backup` (16 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Restauração transacional | `sqlite-backup.repository.test.ts:305` | Coberto |
| Falha no meio não deixa estado parcial | `sqlite-backup.repository.test.ts:305` | Coberto |
| Mensagem oferece tentar de novo | `use-restaurar-backup.test.ts:179` | Coberto |
| Arquivo malformado recusado antes de escrever | `use-restaurar-backup.test.ts:40` | Coberto |
| Combinação por identificador (upsert) | `sqlite-backup.repository.test.ts:237` | Coberto |
| Restauração repetida é idempotente | `sqlite-backup.repository.test.ts:278` | Coberto |
| Movimentos não duplicados | `sqlite-backup.repository.test.ts:278` | Coberto |
| Versão mais nova recusada com mensagem de atualizar | `backup.schema.test.ts:72`; `use-restaurar-backup.test.ts:75` | Coberto |
| Versão anterior aceita e migrada | `backup.schema.test.ts:77` — só verifica a marcação de versão; `CONVERSORES` está vazio (`backup.schema.ts:92`), nenhuma conversão real de forma de dados é exercitada ainda | Parcial (lacuna latente, aceitável hoje com 1 única versão publicada) |
| Arquivo sem versão é recusado | `backup.schema.test.ts:66` | Coberto |
| Reconciliação verifica quantidade vs soma dos movimentos | `sqlite-backup.repository.test.ts:366`; `sqlite-movimento.repository.test.ts:102` | Coberto |
| Divergência é informada | `use-restaurar-backup.test.ts:154`; mas sem CTA/link direto à correção a partir da tela — **ACHADO-039** | Parcial |
| Oferece corrigir sem alteração silenciosa | `use-diagnostico.test.ts:40`; `sqlite-movimento.repository.test.ts:143`; desconectado do fluxo pós-restauração — **ACHADO-039** | Parcial |
| Confirmação explícita com resumo (data + registros) | hook: `use-restaurar-backup.test.ts:93`; UI de confirmação sem teste de componente — **ACHADO-038** | Parcial |
| Cancelar não escreve nada | `use-restaurar-backup.test.ts:133` | Coberto |
| Restauração funciona offline | garantido por ausência estrutural de I/O de rede; sem teste nomeado | Gap (implícito) |

### `tela-de-configuracoes` (8 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Acessível sem navegação profunda | rota de tab (`app/(tabs)/configuracoes.tsx`); sem teste de navegação — **ACHADO-038** | Gap |
| Escolha de tema persistida | lógica herdada de PR-04 (`presentation/theme/provider`); tela sem teste de componente — **ACHADO-038** | Parcial |
| Backup/restaurar/exportar agrupados | `configuracoes.tsx:95-131`; sem teste de componente — **ACHADO-038** | Gap |
| Acesso a diagnóstico | `configuracoes.tsx:133-139`; sem teste — **ACHADO-038** | Gap |
| Restaurar visualmente distinguida (destrutiva) | `configuracoes.tsx:107-117`; sem teste — **ACHADO-038** | Gap |
| Exportar não pede confirmação | `configuracoes.tsx:44-48` (comentário explícito "sem confirmação"); sem teste que afirme a ausência — **ACHADO-038** | Parcial |
| Informa data do último backup / convite ao primeiro | `use-ultimo-backup.test.ts:14`; `data-do-backup.test.ts:4` (lógica); tela sem teste de componente | Parcial |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-038 | média | `app/(tabs)/configuracoes.tsx` sem nenhum teste de componente/E2E — agrupamento, distinção destrutiva, ausência de confirmação no export nunca verificados na composição da tela |
| ACHADO-039 | média | Divergência pós-restauração informada só por toast, sem link direto para a tela de Diagnóstico onde a correção acontece; encadeamento sem teste |
| ACHADO-040 | baixa | `ExpoSistemaDeArquivos` (adaptador real de share/document-picker/file-system) sem teste próprio — só o dublê é exercitado |

**Cobertura**: ~25/35 cenários com teste direto e completo, ~7 parciais (lógica coberta, composição de tela não), 3 gaps puros — todos concentrados na ausência de teste de componente para `configuracoes.tsx` e no adaptador real de sistema de arquivos.
