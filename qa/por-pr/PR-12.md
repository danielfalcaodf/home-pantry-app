---
pr: 12
branch: feat/correcao-navegacao-nativa
commit: 89fc5fe
estado: concluida-com-achado-critico
---

## Foco do checklist

Nenhum header nativo em rota alguma, `BotaoVoltar` presente e ≥48×48 nas telas empilhadas.

## `npm run verificar`

**FALHA — exit code 1.** Ver [[ACHADO-007]] (crítico): `app/_layout.tsx:1` importa `react-native-get-random-values`, mas o pacote não está declarado em `package.json` — `npm ci` limpo não o instala.

## `npm test`

```
Test Suites: 2 failed, 60 passed, 62 total
Tests:       2 failed, 584 passed, 586 total
Time:        8.831 s
```

As 2 falhas são as mesmas de sempre ([[ACHADO-001]]/[[ACHADO-002]], fuso horário) — nenhuma regressão nova em teste, só no lint/dependência.

## Verificações específicas

- **Nenhum header nativo**: `app/_layout.tsx:23` e `app/(tabs)/_layout.tsx:10` — ambos com `headerShown: false`.
- **`BotaoVoltar` presente e testado**: `botao-voltar.test.tsx` — rótulo acessível "Voltar", chama `router.back()` ao tocar.
- **Alvo de toque ≥48×48**: `botao-voltar.tsx` usa `minWidth`/`minHeight: ALVO_TOQUE_MINIMO` importado de `theme/espaco.ts:21`, onde a constante vale `48` — vínculo correto com o token de tema em vez de valor mágico solto no componente. Não há assert direto de estilo no teste do componente (`botao-voltar.test.tsx`), mas o valor está centralizado e nomeado — risco baixo.

## Achados

- [[ACHADO-007]] — **crítico**: `react-native-get-random-values` importado em `app/_layout.tsx` sem estar declarado em `package.json`; `npm run verificar` falha isoladamente. Rastrear se persiste até a PR 13 (topo).

**PR-12 concluída — com achado crítico pendente de rastreamento.**

## F4 — Smoke no emulador

Ambiente reaproveitado das rodadas anteriores: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro já ativos. PR-12 é a correção de navegação nativa — verifiquei diretamente a ausência de header nativo e a geometria do `BotaoVoltar`.

- **ACHADO-007 (crítico, dependência não declarada) — revalidado nesta rodada, não persiste no estado acumulado**: `npm run verificar` (fronteiras + lint + typecheck) passou limpo, exit 0, sem erros. `grep react-native-get-random-values package.json` confirma que a dependência **já está declarada** (`~1.11.0`) no estado acumulado da branch `qa/plano-de-testes` — ou seja, o achado foi corrigido em algum ponto entre a PR-12 e o topo atual da fila (rastreamento pedido pela própria F2 desta PR). Isso não invalida o achado em si (a PR-12 isolada, via `npm ci` limpo, teria falhado) — só confirma que ele não bloqueia o merge no fluxo sequencial em que as PRs estão sendo integradas.
- **Nenhum header nativo (ação real)**: abri "Cadastrar produto" — título "Novo produto" e botão "Voltar" (`←`) renderizados dentro da própria Compose view da aplicação, sem nenhuma barra de ação nativa do Android sobreposta. Coerente com `headerShown: false` em `app/_layout.tsx:23`.
- **`BotaoVoltar` com alvo de toque ≥48×48 (medição real, não apenas nomeada em constante)**: bounds do botão "Voltar" na tela "Novo produto" = `[16,16][64,64]` — exatamente 48×48 px, batendo com `ALVO_TOQUE_MINIMO=48`. Resolve por medição direta a lacuna que a F3 desta PR apontou como "sem assert direto de estilo no teste" (ACHADO-048) — a geometria está correta na prática, só falta o teste automatizado.
- **Confirmação cruzada com rodadas anteriores desta sessão**: os botões "Voltar" observados no detalhe do produto (PR-10) e no modo compra (PR-08) já tinham bounds equivalentes (`[16,4][64,52]` e `[16,16][64,64]` respectivamente, ambos 48×48), reforçando que o padrão é consistente entre as telas empilhadas, não um caso isolado.
- **Verificação de display**: `topResumedActivity=com.triasoftware.repor/.MainActivity`, display único.

Evidência: `qa/por-pr/evidencias/PR-12/smoke-sem-header-nativo-cadastro.png`.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes`, não um checkout isolado do commit `89fc5fe`/`feat/correcao-navegacao-nativa` — em particular, o ACHADO-007 crítico só pôde ser confirmado como "não bloqueante hoje" porque o estado acumulado já inclui a correção de uma PR posterior; um checkout isolado desta PR reproduziria a falha de `npm run verificar`.

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-06-correcao-navegacao-nativa`. 1 capability (`chrome-de-navegacao`), 11 cenários. Nota importante: a própria change documenta (`tasks.md` seção 5) que a verificação foi feita **manualmente** via emulador/screenshots `adb`, não por teste automatizado — isso é coerente com o tipo de bug (chrome visual nativo, difícil de simular em Jest/RNTL) e não é tratado como lacuna por si só, mas os cenários individualmente mapeados abaixo mostram onde a automação está ou não presente.

Nota também: o requisito "Tab bar sem ícones" desta capability foi **modificado** pela change posterior `2026-08-06-ajuste-visual-telas-design-system` (PR-13), que reintroduziu ícones por aba. O código atual (`app/(tabs)/_layout.tsx`) já reflete essa versão mais recente — o cenário original "tab bar sem ícones" está marcado abaixo como substituído, não como divergência, já que a spec ativa hoje é a de PR-13 (auditada na próxima rodada).

| Cenário | Teste | Status |
|---|---|---|
| Header oculto — aba Despensa | `app/_layout.tsx:23` (`headerShown: false` no Stack raiz, cobre todas as rotas incl. abas) | Implementado, sem teste automatizado — **ACHADO-048** |
| Header oculto — Detalhe do produto | mesma config global | Implementado, sem teste — **ACHADO-048** |
| Header oculto — Cadastrar produto | mesma config global | Implementado, sem teste — **ACHADO-048** |
| Header oculto — Modo compra | mesma config global | Implementado, sem teste — **ACHADO-048** |
| Voltar do Detalhe do produto → Despensa | `app/produto/[id].tsx` usa `BotaoVoltar` | `botao-voltar.test.tsx` (chamada de `router.back()`, não a rota de destino específica) | Parcial |
| Voltar do Cadastrar produto → Despensa | `app/produto/novo.tsx` via `FormularioProduto` | mesmo teste genérico do componente | Parcial |
| Voltar do Modo compra → Lista | `app/compra/[id].tsx` usa `BotaoVoltar` | mesmo teste genérico do componente | Parcial |
| Botão de voltar com alvo de toque ≥48×48 e rótulo acessível | `botao-voltar.tsx` (`ALVO_TOQUE_MINIMO=48`, `accessibilityRole="button"`, `accessibilityLabel="Voltar"`) | `botao-voltar.test.tsx` cobre o rótulo acessível; **não** cobre a dimensão mínima — **ACHADO-048** | Parcial |
| Tab bar sem ícones (renderizar) | **substituído** pelo requisito modificado em PR-13 (`ajuste-visual-telas-design-system`) — código atual já tem ícones por aba | N/A (spec superada, ver PR-13) |
| Tab bar sem ícone de aviso `MissingIcon` | idem — resolvido de outra forma pela PR-13 (ícone real por aba, não ausência de ícone) | N/A (spec superada) |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-048 | baixa | `headerShown: false` e alvo de toque de 48×48 do `BotaoVoltar` sem teste automatizado — verificação desta PR foi só manual |

**Cobertura**: capability pequena e majoritariamente verificada manualmente por design (documentado na própria change); `BotaoVoltar` tem teste de comportamento (rótulo + `back()`) mas não de geometria; requisito de tab bar foi substituído pela PR seguinte antes mesmo desta auditoria acontecer.
