---
pr: 12
branch: feat/correcao-navegacao-nativa
commit: 89fc5fe
estado: concluida-com-achado-critico
---

## Foco do checklist

Nenhum header nativo em rota alguma, `BotaoVoltar` presente e ≥48×48 nas telas empilhadas.

## `npm run verificar`

**FALHA — exit code 1.** Ver [[ACHADO-007]] (crítico): `app/_layout.tsx:1` importa `react-native-get-random-values`, mas o pacote não está declarado em `package.json` — `npm ci` limpo não o instala.

## `npm test`

```
Test Suites: 2 failed, 60 passed, 62 total
Tests:       2 failed, 584 passed, 586 total
Time:        8.831 s
```

As 2 falhas são as mesmas de sempre ([[ACHADO-001]]/[[ACHADO-002]], fuso horário) — nenhuma regressão nova em teste, só no lint/dependência.

## Verificações específicas

- **Nenhum header nativo**: `app/_layout.tsx:23` e `app/(tabs)/_layout.tsx:10` — ambos com `headerShown: false`.
- **`BotaoVoltar` presente e testado**: `botao-voltar.test.tsx` — rótulo acessível "Voltar", chama `router.back()` ao tocar.
- **Alvo de toque ≥48×48**: `botao-voltar.tsx` usa `minWidth`/`minHeight: ALVO_TOQUE_MINIMO` importado de `theme/espaco.ts:21`, onde a constante vale `48` — vínculo correto com o token de tema em vez de valor mágico solto no componente. Não há assert direto de estilo no teste do componente (`botao-voltar.test.tsx`), mas o valor está centralizado e nomeado — risco baixo.

## Achados

- [[ACHADO-007]] — **crítico**: `react-native-get-random-values` importado em `app/_layout.tsx` sem estar declarado em `package.json`; `npm run verificar` falha isoladamente. Rastrear se persiste até a PR 13 (topo).

**PR-12 concluída — com achado crítico pendente de rastreamento.**
