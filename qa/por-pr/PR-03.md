---
pr: 3
branch: change/persistencia-sqlite
commit: e93c8b4
estado: concluida
---

## Foco do checklist

Projeto `infra` com SQLite real: PRAGMA FK ligada, migrations desde vazio **e** desde cada versão intermediária, transação de baixa atômica, append-only, índice parcial de faltantes, unicidade de compra aberta.

## `npm run verificar`

Fronteiras e typecheck limpos. **1 warning de lint** (não erro) — ver [[ACHADO-005]].

## `npm test`

```
Test Suites: 16 passed, 16 total
Tests:       163 passed, 163 total
Time:        1.187 s
```

## Verificações específicas

- **PRAGMA FK ligada**: `src/infrastructure/db/client.ts:16` (`PRAGMA foreign_keys = ON`) + autoverificação que lança erro se não pegou (`client.ts:23-25`); testado em `client.test.ts:4` ("foreign_keys está ligado").
- **Migrations desde vazio**: `migrations.test.ts:22` ("migration 0000_init sobre banco vazio") — 6 tabelas, 7 índices, índice de histórico decrescente, unicidade de nome insensível a caixa.
- **Migrations desde cada versão intermediária**: **não aplicável ainda nesta PR** — só existe a migration `0000_init`; não há versão intermediária para testar. Não é lacuna, é esperado no início da cadeia de migrations (revisitar a partir da PR onde a segunda migration aparecer).
- **Transação de baixa atômica**: `sqlite-movimento.repository.ts:65` — `this.db.transaction(...)` envolvendo a leitura/gravação.
- **Índice parcial de faltantes**: `migrations.test.ts:219` — "a consulta de faltantes usa `idx_produto_em_falta`, não varredura completa".
- **Unicidade de compra aberta**: `migrations.test.ts:152` — "segunda compra aberta na mesma casa é rejeitada pelo índice único".
- **Append-only**: não localizado teste explícito nesta PR que impeça `UPDATE`/`DELETE` em `movimento_estoque` — a confirmar na F3 (auditoria de conformidade) se está coberto em PR posterior.

## Achados

- [[ACHADO-004]] — `backup-pre-migration.ts` (introduzido nesta PR) sem nenhum teste; caminho crítico de recuperação de dados.
- [[ACHADO-005]] — import não usado `Milesimos` em `sqlite-produto.repository.ts:7` (warning de lint).

**PR-03 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-persistencia-sqlite/`. Três capabilities auditadas (~55 cenários). `qa-expert` seguiu indisponível como `subagent_type`; auditoria conduzida diretamente. **Nota**: o estado atual do repositório tem 4 migrations (`0000_init` … `0003_compra_item_atualizar_preco`), ao contrário do que existia quando a PR-03 foi originalmente testada (só `0000_init`) — isso reabre o cenário "aplicação a partir de versão intermediária", que antes era N/A.

### Capability: `banco-local`

| Cenário | Teste/evidência | Status |
|---|---|---|
| Chaves estrangeiras ligadas | `client.test.ts:4` | coberto |
| Órfão é rejeitado | `client.test.ts:9` | coberto |
| Modo WAL ativo | `client.ts:15` seta `PRAGMA journal_mode = WAL`, mas nenhum teste consulta o PRAGMA de volta para confirmar | **descoberto** |
| Conexão é singleton | `client.ts:8-10` — módulo com instância única por construção (import cacheado pelo Node/Metro), mas nenhum teste importa o cliente por dois caminhos e compara identidade | **descoberto** |
| Escuta de mudanças habilitada | `client.ts:10` (`enableChangeListener: true`) e `observador.ts` (`addDatabaseChangeListener`) existem, mas não há teste (`src/infrastructure/db/observador.test.ts` não existe) | **descoberto** |
| Quantidade e dinheiro são inteiros | `schema.ts` declara colunas `integer(...)`; sem teste dedicado, mas `migrations.test.ts:24` confirma schema aplicado igual ao declarado | coberto (config + indireto) |
| Chave primária textual UUID | gerado por `gerarId()` (já testado em ACHADO-009/PR-01) na camada de repositório; sem teste no nível de banco que valide o formato da PK gravada | coberto (indireto via camada de aplicação) |
| Restrição de quantidade não negativa | `migrations.test.ts:83` (`it.each` "produto: quantidade atual negativa") | coberto |
| Restrição de coerência de sinal do movimento | `migrations.test.ts:117,123` ("movimento: baixa com sinal positivo" / "reposição com sinal negativo") | coberto |
| Restrição de variação não nula | `migrations.test.ts:111` ("movimento: variação zero") | coberto |
| Item de compra é de produto ou avulso | `migrations.test.ts:167` | coberto |
| Nome duplicado é rejeitado | `migrations.test.ts:56`, `sqlite-produto.repository.test.ts:41` | coberto |
| Nome reutilizável após remoção lógica | `sqlite-produto.repository.test.ts:51` | coberto |
| Índice parcial de faltantes é utilizado | `migrations.test.ts:239` | coberto |
| No máximo uma compra aberta por casa | `migrations.test.ts:153`, `sqlite-compra.repository.test.ts:41,374` (inclusive sob concorrência) | coberto |
| Índices de sincronização ausentes | `migrations.test.ts:35` ("cria os sete índices do MVP e nenhum sobre sync_status") | coberto |
| Migration aplicada com sucesso (app abre, migrations pendentes aplicam antes da tela) | `src/composicao/banco.ts` (`usePrepararBanco`) e `app/_layout.tsx:33-46` implementam o fluxo, mas não há teste (nem RTL nem E2E) que monte `RootLayout` com uma migration pendente e confirme a sequência | **descoberto (bloqueado por F4/F5 para E2E; poderia ter teste RTL unitário e não tem)** |
| Falha de migration é visível | `TelaErro` component existe e está cabeado em `_layout.tsx:47-56`, mas nenhum teste simula `useMigrations` retornando erro e confirma que a tela aparece | **descoberto** |
| Aplicação desde o schema vazio | `migrations.test.ts:24` | coberto |
| Aplicação a partir de versão intermediária | 4 migrations existem hoje (`0000`-`0003`), mas nenhum teste aplica só um subconjunto e depois o restante para confirmar convergência ao mesmo schema final | **descoberto (só virou testável depois da PR-03; não é regressão desta PR, mas é lacuna presente hoje)** |
| Backup antes de migrar | `backup-pre-migration.ts` existe e é chamado em `banco.ts:14`, mas **sem nenhum teste** — já registrado em [[ACHADO-004]], não duplicado aqui | descoberto (já rastreado) |

### Capability: `dados-iniciais`

| Cenário | Teste | Status |
|---|---|---|
| Casa criada uma única vez | `seed.test.ts:8` | coberto |
| Aberturas seguintes não duplicam | `seed.test.ts:8` (mesmo teste, "e não duplica nas seguintes") | coberto |
| Consultas filtram por casa | todo repositório recebe `casaId` como parâmetro obrigatório (assinatura de `ProdutoRepository`, `MovimentoRepository`, `CompraRepository`) e os testes de "não retorna de outra casa" aparecem em múltiplos arquivos (`sqlite-produto.repository.test.ts:420`, `sqlite-compra.repository.test.ts:816,852`) | coberto |
| Lista disponível offline | `seed.test.ts:21`; reforçado por `offline.test.ts:27` (nenhum arquivo de código-fonte usa API de rede) | coberto |
| Categorias já normalizadas | `seed.test.ts:37` | coberto |
| Itens criados sem quantidade | `seed.test.ts:21` (quantidade atual 0, necessária sugerida > 0) | coberto |
| Adoção é escolha do usuário, não automática | `seed.test.ts:31` | coberto |
| Adoção parcial | `seed.test.ts:37` | coberto |

### Capability: `repositorios`

| Cenário | Teste/evidência | Status |
|---|---|---|
| Interfaces separadas por agregado | `src/ports/produto.repository.ts`, `movimento.repository.ts`, `compra.repository.ts` — três arquivos distintos, sem interface unificada | coberto (estrutural) |
| Caso de uso depende do contrato | `eslint.config.js` `boundaries/dependencies` (mesma regra auditada na PR-01) impede `application/` de importar `infrastructure/` | coberto (config, mesma evidência da PR-01) |
| Implementação substituível | casos de uso em `application/` usam repositório fake nos próprios testes (padrão observado em toda a suíte `application`) | coberto (estrutural/arquitetural) |
| Acesso direto ao banco é bloqueado fora da infra | mesma regra `boundaries/dependencies`, policy `presentation`/`application` não lista `infrastructure` — evidência já auditada na PR-01 | coberto (config, mesma evidência) |
| Ordenação por estado (despensa) | `sqlite-produto.repository.test.ts:100` | coberto |
| Ordem alfabética dentro do grupo, ignorando caixa | mesmo teste (`:100`), nomes com colisão de estado | coberto |
| Removidos e inativos são excluídos | `sqlite-produto.repository.test.ts:84-91` (removido) + `:406-413`/`:470,480` (inativo, via `listarTudoParaBackup`/`valorBrutoDoEstoque` que contrastam explicitamente com `listarDespensa`) | coberto |
| Colunas explícitas na consulta da despensa | **`listarDespensa` em `sqlite-produto.repository.ts:158-160` usa `.select()` sem especificar colunas**, ao contrário de `listarFaltantes` (`:181-189`) que nomeia cada coluna explicitamente. O spec exige colunas nomeadas especificamente para a consulta da despensa. Ambíguo se a intenção do spec é sobre a forma SQL literal (Drizzle gera nomes de coluna mesmo com `.select()` vazio, nunca `SELECT *` literal) ou sobre a prática de nomear no código-fonte — mas o código diverge do padrão que o restante do arquivo segue | **descoberto (revisão humana recomendada — pode ser falso positivo se a leitura do spec for só sobre SQL literal)** |
| Diferença bruta retornada sem arredondar | `sqlite-produto.repository.test.ts:110` | coberto |
| Arredondamento acontece no domínio, não na consulta | a consulta (`listarFaltantes`) não chama nenhuma função de arredondamento — só `domain/produto/estoque.rules.ts` faz isso (já auditado na PR-02); ausência de arredondamento na query confirma por omissão, sem teste negativo dedicado | coberto (por inspeção) |
| Predicado casa com índice parcial | `migrations.test.ts:239` | coberto |
| Escrita conjunta (baixa) | `sqlite-produto.repository.test.ts:146` | coberto |
| Falha reverte tudo | `sqlite-produto.repository.test.ts:177` | coberto |
| Saldo resultante reflete valor real | `sqlite-produto.repository.test.ts:146` (assert de `quantidade_resultante`) | coberto |
| Baixa não deixa quantidade negativa | `sqlite-produto.repository.test.ts:201` | coberto |
| Situação de sincronização marcada como pendente | `sqlite-produto.repository.test.ts:174` (`sync_status` = `'pendente'`) | coberto |
| Contrato sem escrita destrutiva (movimento) | `src/ports/movimento.repository.ts` — interface não declara `atualizar`/`remover`, só `historicoPor*`, `desfazer`, `reconciliar`, `corrigir*`, `listarTudoParaBackup` | coberto (estrutural) |
| Desfazer insere movimento inverso | `sqlite-movimento.repository.test.ts:69` | coberto |
| Histórico mais recente primeiro | `sqlite-movimento.repository.test.ts:47,61` | coberto |
| Compra inteira ou nada (falha no meio) | `sqlite-compra.repository.test.ts:263` | coberto |
| Reposição aplicada a todos os marcados | `sqlite-compra.repository.test.ts:202` | coberto |
| Itens não marcados permanecem sem efeito | `sqlite-compra.repository.test.ts:302` | coberto |
| Total pago calculado na finalização | `sqlite-compra.repository.test.ts:202` | coberto |
| Situação finalizada exige data | `migrations.test.ts:140` | coberto |
| Uma consulta para todos os itens (sem N+1) | `sqlite-compra.repository.test.ts:128,633` | coberto |
| Item avulso não desaparece | `sqlite-compra.repository.test.ts:128,327` | coberto |

### Achados desta rodada

- Ver `qa/achados/ACHADO-011.md` — cluster de garantias de conexão (WAL, singleton, escuta de mudanças) sem teste dedicado.
- Ver `qa/achados/ACHADO-012.md` — fluxo de preparação do banco (`_layout.tsx`/`usePrepararBanco`) sem teste que exercite sucesso ou falha de migration.
- Ver `qa/achados/ACHADO-013.md` — "aplicação a partir de versão intermediária" agora testável (4 migrations existem) e ainda sem teste.
- Ver `qa/achados/ACHADO-014.md` — `listarDespensa` usa `.select()` sem colunas nomeadas, possivelmente divergindo do requisito "colunas explícitas"; sinalizado para revisão humana por ambiguidade de interpretação do spec, não corrigido aqui.
- [[ACHADO-004]] (já existente) reafirmado: `backup-pre-migration.ts` ainda sem teste, cobre diretamente o cenário "Backup antes de migrar".

**Resumo**: ~50/55 cenários cobertos; 5 lacunas novas + 1 já rastreada (ACHADO-004).
