---
pr: 5
branch: change/despensa-e-cadastro-produto
commit: 0044c64
estado: concluida-com-achado-critico
---

## Foco do checklist

Ordenação por estado, filtro com contagem, busca sem acento/caixa, nome duplicado, remoção lógica, medidor com 3 canais redundantes, adoção da lista base atômica.

## `npm run verificar`

**FALHA — exit code 1.** Ver [[ACHADO-006]] (severidade crítica): 2 erros de lint `react-hooks/set-state-in-effect` em `use-categorias.ts:28` e `use-produtos.ts:69`. O baseline do topo (F1) está verde, então isso foi corrigido em alguma PR posterior — a confirmar em qual, prosseguindo a F2.

## `npm test`

```
Test Suites: 24 passed, 24 total
Tests:       279 passed, 279 total
Time:        3.305 s
```

Todos os testes passam (o lint é o único gate vermelho aqui).

## Verificações específicas

- **Medidor "linha d'água" com 3 canais redundantes**: `medidor-e-item.test.tsx:29-51` — parcial (tinta + opacidade do tema), zerado (nenhuma tinta, só régua na base em crítico — "o vazio é o sinal"), cheio (tinta em altura total, régua rente ao topo sem extravasar), com sobra (traço fino, sem número/etiqueta).
- **`ItemDespensa` acessível**: `:71,76,84` — anuncia nome+leitura+estado juntos; botão anuncia ação completa (nunca só "menos"); item zerado mantém botão no layout, só esmaecido.
- **Sem aritmética de domínio no componente**: `:103-112` — teste de conformidade lê o próprio arquivo-fonte via `fs.readFileSync` e garante que `medidor-nivel.tsx`/`item-despensa.tsx` não fazem aritmética sobre `quantidade*` nem referenciam `quantidadeNecessaria` — força o cálculo a ficar no domínio.
- **Sem gesto de swipe na linha**: `:115` — mesma técnica de leitura de fonte, garante ausência de `Swipe|PanGesture|onSwipe|Swipeable` em `item-despensa.tsx` (regra do CLAUDE.md: "gesto invisível não pode carregar a ação principal do app").
- **Adoção da lista base atômica**: `use-lista-base.test.ts:29,39,50` — cria só os itens escolhidos, adoção vazia não falha, falha em bloco não deixa despensa parcial e permite retry.
- **Ordenação/filtro/busca/duplicidade/remoção lógica**: não localizados testes com esses nomes explícitos nos arquivos revisados nesta rodada — a confirmar na F3 (auditoria de conformidade contra os specs), pode estar em arquivo de `use-produtos`/`use-despensa` não aberto nesta passada.

## Achados

- [[ACHADO-006]] — **crítico**: `npm run verificar` falha isoladamente nesta PR (2 erros de lint `react-hooks/set-state-in-effect`), embora o topo da cadeia esteja verde.

**PR-05 concluída — com achado crítico pendente de rastreamento (em qual PR foi corrigido).**

## F4 — Smoke no emulador

Ambiente reaproveitado das rodadas anteriores: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro já ativos. PR-05 introduz a tela Despensa (medidor "linha d'água", filtros, busca), então explorei além do piso mínimo.

- **Flow base**: `.maestro/smoke-launch.yaml` — passou.
- **Filtro por estado**: tap em "Acabou, 37" seleciona o chip e reordena a lista alfabeticamente, todos os itens exibidos com rótulo textual de estado (ex.: "Alho, 0 de 2 un, Acabou") — confirma o canal textual redundante do medidor em runtime. Evidência: `qa/por-pr/evidencias/PR-05/smoke-filtro-acabou.png`.
- **Busca sem acento/caixa**: digitar "acucar" (sem acento) no campo de busca encontrou e exibiu "Açúcar" — confirma a normalização de busca funcionando em runtime, não só no teste unitário. Evidência: `qa/por-pr/evidencias/PR-05/smoke-busca-sem-acento.png`.
- **Medidor**: elementos `medidor-regua`/`medidor-tinta`/`medidor-sobra` presentes na view hierarchy dos itens parciais (ex.: Frango, "Cheio"), confirmando que o medidor renderiza suas camadas visuais.
- **Verificação de display**: `topResumedActivity=com.triasoftware.repor/.MainActivity`, display único.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes`, não um checkout isolado do commit `0044c64`/`change/despensa-e-cadastro-produto`. A tela vista já reflete refinamentos de PRs posteriores (ex.: ajuste-visual-telas-design-system, PR-13).

**Nota sobre ACHADO-006**: o smoke test não testa `npm run verificar` (isso é lint estático, não comportamento em runtime) — o achado crítico permanece sem relação com o resultado desta F4; a tela funciona normalmente apesar do lint falhar isoladamente no commit original da PR.

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-despensa-e-cadastro-produto/`. Quatro capabilities auditadas (~60 cenários). `qa-expert` seguiu indisponível como `subagent_type`; auditoria conduzida diretamente. Esta rodada resolve a pendência que a própria PR-05.md deixou aberta na F2 ("ordenação/filtro/busca/duplicidade/remoção lógica — a confirmar na F3").

### Capability: `adocao-da-lista-base`

| Cenário (agrupado por requisito) | Teste | Status |
|---|---|---|
| Convite exibido / não bloqueia cadastro manual | `componentes-base.test.tsx:100` (EstadoVazio genérico) cobre o mecanismo; wiring específico da despensa vazia está em `app/(tabs)/index.tsx`, sem teste de tela | coberto (mecanismo) / descoberto (wiring de tela) |
| Adoção completa / parcial / mínima / nenhum item | `use-lista-base.test.ts:29,39,50` | coberto (3/4 diretos; "mínima permitida" é caso particular de "parcial", mesma lógica) |
| Criação em bloco / falha não deixa parcial / offline | `use-lista-base.test.ts:50`; offline coberto indiretamente por `offline.test.ts` (nenhuma API de rede em todo o código-fonte, já auditado na PR-03) | coberto |
| Estado inicial dos adotados / item crítico / edição livre | `seed.test.ts:21` (já auditado na PR-03, mesma lista base) cobre estado inicial; "aparece como crítico" e "edição livre" decorrem do próprio mecanismo de `estadoDoItem` (PR-02) e `useEditarProduto` (PR-05, sem restrição especial para itens adotados) — sem teste que una os três especificamente para um item adotado | coberto (por composição de peças já testadas), sem teste de integração dedicado |

### Capability: `cadastro-de-produto`

| Cenário (agrupado por requisito) | Teste | Status |
|---|---|---|
| Cadastro mínimo / campos opcionais recolhidos | `hooks.test.ts:104` cobre o cadastro (lógica); **`formulario-produto.tsx` não tem nenhum arquivo de teste** — o comportamento de UI "campos opcionais só aparecem ao expandir" não é verificado | coberto (lógica) / **descoberto (componente)** |
| Quantidade necessária decimal / zero rejeitada / nome vazio rejeitado | `hooks.test.ts:75` (nome vazio); zero rejeitada e decimal já cobertos no domínio (PR-02, `validacao.test.ts`) e reafirmados aqui pela mesma validação reusada em `useCadastrarProduto` | coberto |
| Vocabulário do rótulo de quantidade necessária | sem teste que verifique o texto do rótulo renderizado (`formulario-produto.tsx` sem teste, ver acima) | **descoberto** |
| Sugestões vêm dos dados / escolher existente evita variante / categoria nova normalizada / texto cru nunca gravado | `formulario-produto.tsx:69-73` implementa o filtro de sugestões, mas sem teste; normalização em si já coberta no domínio (PR-02) e em `hooks.test.ts:104` (categoria `' grãos '` → `'Grãos'`) | coberto (normalização) / **descoberto (comportamento de sugestão na UI)** |
| Duplicidade detectada antes de salvar / salvar exige nome diferenciado / diferença de caixa também é duplicidade | `hooks.test.ts:90` cobre a mensagem e a rejeição (inclusive caixa, via `'arroz'` vs `'Arroz'`); a ação "ver o item existente" é implementada em `app/produto/novo.tsx:34-38` (`duplicado` state) mas sem teste de tela | coberto (lógica/mensagem) / **descoberto (ação "ver item existente")** |
| Nome de item removido pode ser reutilizado | já coberto no repositório (PR-03, `sqlite-produto.repository.test.ts:51`) | coberto |
| Quantidade em destaque / edição persistida / alterar necessária muda estado | `hooks.test.ts:122,130` (`useProduto`/`useEditarProduto`) cobrem a lógica; exibição em papel de display é responsabilidade da tela `app/produto/[id].tsx`, sem teste | coberto (lógica) / descoberto (apresentação, tela sem teste) |
| Resumo do histórico no rodapé | `use-resumo-historico.ts`/`.test.ts` existe e cobre a lógica de resumo; wiring na tela de detalhe sem teste | coberto (lógica) |
| Item removido some da despensa / histórico preservado / remoção pede confirmação | `hooks.test.ts:147` cobre "remoção lógica tira o item da despensa"; histórico preservado já coberto no repositório (PR-03: remoção lógica não apaga `movimento_estoque`, é append-only por design); confirmação antes de remover é UI (`item-despensa.tsx`/tela de detalhe), sem teste que force o fluxo de confirmação | coberto (lógica) / descoberto (confirmação de UI) |

### Capability: `medidor-linha-dagua`

Praticamente já verificado na própria PR-05.md (seção "Verificações específicas"), reconferido aqui contra o spec formal:

| Cenário (agrupado) | Teste | Status |
|---|---|---|
| Preenchimento proporcional / régua na superfície / zerado sem tinta / cheio não transborda / sobra discreta | `medidor-e-item.test.tsx:29-51` | coberto |
| Sem contêiner visual (sem card/borda/sombra) | não localizado um teste que afirme ausência explícita de sombra/borda arredondada na linha — o teste de conformidade (`:103-112`) cobre ausência de aritmética e de swipe, não estilo de contêiner | **descoberto** |
| Componente recebe valores prontos / fração já limitada na origem | `medidor-e-item.test.tsx:103-112` (leitura de fonte, sem aritmética de domínio) | coberto |
| Rótulo textual presente / legível sem cor | `medidor-e-item.test.tsx:71,76` | coberto |
| Toque na esquerda abre detalhe / alvo do botão reservado quando inativo / sem swipe / leitura tabular | swipe coberto (`:115`); alvo reservado coberto (`:84`, "item zerado mantém botão no layout, só esmaecido"); toque abrindo detalhe e leitura tabular não localizados neste arquivo — podem estar em `item-despensa.tsx` sem teste correspondente de navegação | coberto (2/4) / **descoberto (toque→navegação, leitura tabular explícita)** |
| Rótulo acessível da linha / rótulo do botão completo | `medidor-e-item.test.tsx:71,76` (linha); `:76` cobre o botão não anunciar só "menos" | coberto |
| Sem animação na rolagem / sem entrada em cascata | consistente com "Movimento respeita a preferência de acessibilidade" (PR-04, ACHADO-019) — mesma ausência de teste de animação | descoberto (mesma classe do ACHADO-019, não duplicado) |

### Capability: `tela-despensa`

| Cenário (agrupado) | Teste | Status |
|---|---|---|
| Ordenação por estado (críticos, em falta, ok) + alfabética | já coberto no repositório (PR-03, `sqlite-produto.repository.test.ts:100`) e reafirmado por `hooks.test.ts:36` ("entrega estado, fração e rótulo já calculados") | coberto |
| Agrupamento por categoria no filtro "Tudo" / sem cabeçalho nos demais / itens sem categoria agrupados | `agrupar-despensa.test.ts:53` (`agruparPorCategoria`) cobre agrupamento e categoria nula; "cabeçalho fixo durante rolagem" é comportamento de `FlashList`/`stickyHeaderIndices`, não testável em Jest sem simular scroll — dependente de F6 | coberto (lógica de agrupamento) / descoberto (comportamento sticky visual, dependente de F4/F6) |
| Contagens por estado / filtro aplicado / atualiza sem recarregar / chip sem itens | `agrupar-despensa.test.ts:11,26` (`contarPorEstado`), `:35` (`casaComFiltro`); "atualiza sem recarregar" decorre de `useLiveQuery` (mecanismo já coberto por `hooks.test.ts:51`, "reconsulta quando o banco avisa que mudou") | coberto |
| Categorias oferecidas vêm dos dados / combinação com filtro de estado | `hooks.test.ts:165` (`useCategorias`) cobre a origem das categorias; combinação de filtro de categoria + estado simultâneos não localizada em teste (só estado isolado em `agrupar-despensa.test.ts`) | coberto (parcial) / **descoberto (combinação categoria+estado)** |
| Busca parcial / ignora acento e caixa / sem resultado oferece cadastro | `sqlite-produto.repository.test.ts:121` cobre busca parcial (banco); `normalizar-busca.ts` (`casaComBusca`/`normalizarParaBusca`) implementa a normalização de acento usada em `app/(tabs)/index.tsx:95`, mas **não tem teste próprio** (`normalizar-busca.test.ts` não existe); "sem resultado oferece cadastro" é UI de `app/(tabs)/index.tsx`, sem teste | coberto (busca parcial no banco) / **descoberto (normalização de acento sem teste unitário; UI de "sem resultado" sem teste)** |
| Nova escrita reflete na lista / sem segunda fonte de verdade | `hooks.test.ts:51` (reconsulta via `useLiveQuery`); "sem segunda fonte de verdade" é arquitetural — nenhum `useState`/cache de lista de produtos fora do hook, mas sem teste de conformidade dedicado (diferente do teste de fronteiras que existe para outras regras) | coberto (mecanismo) / descoberto (regra "sem segunda fonte" sem teste de conformidade que a force) |
| Rolagem fluida com 300 itens / virtualização acima de 150 | `app/(tabs)/index.tsx:1,237` usa `FlashList` (confirma virtualização por config), mas nenhum teste de performance ou de volume — inerentemente E2E/F6 | descoberto (dependente de F4/F6, consistente com outras métricas de desempenho já sinalizadas) |

### Achados desta rodada

- Ver `qa/achados/ACHADO-022.md` — `formulario-produto.tsx` sem nenhum teste (campos recolhidos, sugestões de categoria, rótulo de vocabulário).
- Ver `qa/achados/ACHADO-023.md` — `normalizar-busca.ts` (normalização de acento usada na busca) sem teste unitário próprio.
- Ver `qa/achados/ACHADO-024.md` — ação "ver item existente" no fluxo de duplicidade (`app/produto/novo.tsx`) sem teste.
- Ver `qa/achados/ACHADO-025.md` — ausência de teste que afirme explicitamente "sem card/borda/sombra" na linha da despensa (contêiner visual).

**Resumo**: cobertura predominantemente forte na camada de hooks/domínio (reaproveitando testes já auditados nas PRs 2 e 3); as lacunas concentram-se nos componentes de tela (`formulario-produto.tsx`, telas em `app/`) e em duas funções puras não testadas diretamente (`normalizar-busca.ts`).
