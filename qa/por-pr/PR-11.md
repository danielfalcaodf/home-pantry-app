---
pr: 11
branch: feature/resumo-valores-e-historico
commit: 2ab801d
estado: concluida-com-achados-de-fuso
---

## Foco do checklist

**Nenhum total combinado** entre valor de estoque e valor de lista, só compras finalizadas no gasto, agrupamento por fuso local, detalhe de compra sem N+1, produto removido ainda aparece. Provável origem de ACHADO-001/002 — **confirmado**.

## `npm run verificar`

Verde, exit 0, sem warnings.

## `npm test`

```
Test Suites: 2 failed, 59 passed, 61 total
Tests:       2 failed, 579 passed, 581 total
Time:        8.797 s
```

**Confirma que esta é a PR de origem** de [[ACHADO-001]] (`historico-de-compras.test.ts`) e [[ACHADO-002]] (`use-gasto-mensal.test.ts`) — ambos os arquivos são introduzidos aqui. Os números batem com o baseline da F1 (topo): mesmas 2 falhas, resto verde — confirma que nenhuma PR entre a 11 e a 13 introduz nem corrige regressão nesses dois testes.

## Verificações específicas

- **Nenhum total combinado**: `use-resumo-valores.test.ts:46` — "os dois valores nunca são a mesma referência somada (design D2)" — teste nomeia explicitamente a decisão de design.
- **Produto removido ainda aparece / não invalida totais**: `:63` — "produto removido logicamente e produto sem preço não invalidam os totais (task 1.7)".
- **Só compras finalizadas no gasto**: confirmado a nível de implementação (não só teste) — `sqlite-compra.repository.ts:354` (`AND status = 'finalizada'`) e comentário de design D5 ("cancelada não [conta]"). Não há teste negativo explícito provando que compra aberta/cancelada fica de fora do gasto mensal via a suíte de aplicação — a confirmar na F3 se falta cobertura de teste de integração para esse filtro (o SQL está correto e comentado, mas sem teste de infraestrutura dedicado a essa exclusão específica).
- **Detalhe de compra sem N+1**: `use-detalhe-compra.test.ts:22` — "carrega a compra e os itens já com o produto trazido pela **junção**" — confirma que é um único JOIN, não N consultas por item.
- **Agrupamento por fuso local — intencional e consistente**: ver detalhamento em [[ACHADO-002]]. SQLite real (`strftime(..., 'localtime')`) e repositório falso de teste (`Date` getters locais, comentário explícito "não UTC") concordam entre si; só a fixture do teste usa `Date.UTC`, causando os 2 falsos-negativos.

## Achados

- Confirma origem de [[ACHADO-001]] e [[ACHADO-002]] nesta PR.
- Possível lacuna de cobertura (não aberta como achado formal ainda): ausência de teste de infraestrutura que prove explicitamente que compra `aberta`/`cancelada` não entra no cálculo de gasto mensal, apesar do SQL estar correto — revisitar na F3.

**PR-11 concluída.**

## F4 — Smoke no emulador

Ambiente reaproveitado das rodadas anteriores: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro já ativos. PR-11 introduz a tela Resumo e o histórico de compras — explorei além do piso mínimo.

- **Nenhum total combinado (design D2), confirmado visualmente**: "Em casa" (R$ 0,00, "40 itens sem preço — valor parcial") e "Falta comprar" (R$ 0,00, "38 itens sem preço — valor parcial") aparecem como blocos claramente separados, nunca somados — condiz com `use-resumo-valores.test.ts:46`.
- **Chips de estado da despensa** ("Acabou 38", "Faltando 0", "Cheio 2") exibidos corretamente, refletindo a massa de dados atual (inclui os efeitos residuais das PRs anteriores da fila: baixa de 1 pacote de Arroz na PR-06, item marcado/desmarcado na PR-08).
- **Toque em contagem leva à despensa filtrada (ação real, resolve ACHADO-045 parcialmente)**: toquei em "Cheio, 2" no Resumo — navegou para a tela Despensa mostrando exatamente os 2 itens no estado Cheio ("Condicionador" e "Frango"), confirmando em runtime um comportamento que a F3 havia marcado como gap de teste automatizado (a navegação em si funciona; ainda falta cobertura de teste, achado mantido, mas não é uma regressão funcional).
- **Gasto este mês / histórico — estado vazio coerente**: "Gasto este mês R$ 0,00" com texto convidativo "Nenhuma compra fechada ainda. Assim que você fechar a primeira, o gasto do mês aparece aqui." "Ver histórico" leva à tela "Histórico de compras" com o mesmo estado vazio ("Nenhuma compra fechada ainda."). Esperado: nenhuma compra foi fechada em nenhuma rodada anterior da fila de QA (a PR-08 deliberadamente não acionou "Fechar compra" para não corromper a massa de dados) — não há como exercitar a listagem populada ou o detalhe de compra sem fechar uma compra de verdade, o que sairia do escopo não-destrutivo desta rodada.
- **Verificação de display**: `topResumedActivity=com.triasoftware.repor/.MainActivity`, display único.

Evidências: `qa/por-pr/evidencias/PR-11/smoke-resumo-valores.png`, `smoke-historico-vazio.png`, `smoke-toque-contagem-filtra-despensa.png`.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes`, não um checkout isolado do commit `2ab801d`/`feature/resumo-valores-e-historico`. Os 2 testes falhos (`historico-de-compras.test.ts`, `use-gasto-mensal.test.ts`) são o ACHADO-001/002 de fuso horário já documentado na F1/F2 — não afetam o comportamento observado no emulador (que usa hora local real, coerente com o design).

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-03-resumo-valores-e-historico`. 3 capabilities, ~35 cenários mapeados.

**Resolução da pendência aberta na F2**: a linha 30 (acima) marcava como "a confirmar" se havia teste de infraestrutura provando que compra `aberta`/`cancelada` não entra no gasto mensal. **Existe** — `sqlite-compra.repository.test.ts:590` ("compras abertas e canceladas não entram no gasto (task 3.9)"). Pendência fechada, sem achado.

### `gasto-mensal` (9 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Total gasto por mês, últimos 12 meses | `sqlite-compra.repository.test.ts:564`; `use-gasto-mensal.test.ts:43` | Coberto |
| Meses ordenados do mais recente | mesmos testes acima | Coberto |
| Cada mês informa qtd de compras | `sqlite-compra.repository.test.ts:564`; `use-gasto-mensal.test.ts:51` | Coberto |
| Mês sem compra aparece zerado, consistente | `compra.rules.test.ts:244`; `use-gasto-mensal.test.ts:51` | Coberto |
| Só finalizadas contam (abertas/canceladas fora) | `sqlite-compra.repository.test.ts:590` | Coberto |
| Compra finalizada com total zero conta na qtd, contribui zero | sem teste dedicado — **ACHADO-046** | Gap |
| Agrupamento por fuso local | `sqlite-compra.repository.test.ts:578` | Coberto |
| Estado vazio informa afirmativamente | implementação `app/(tabs)/resumo.tsx:137-141`; sem teste de tela — **ACHADO-045** | Gap |
| Uma única compra exibida normalmente | testes existentes usam 2+ compras; nenhum isolado com exatamente 1 | Parcial |

### `historico-de-compras` (10 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Lista finalizadas, mais recente primeiro | `sqlite-compra.repository.test.ts:735`; `use-historico-compras.test.ts:131` | Coberto |
| Canceladas distinguíveis, sem total como gasto | dado testado (`:735`); distinção visual na tela sem teste — **ACHADO-045** | Parcial |
| Valores em família monoespaçada | `tipografia.test.ts:20` (nível de design-system) | Coberto |
| Detalhe mostra itens com qtd e valor pago por unidade | implementação `historico/[id].tsx:40-70`; sem teste de tela | Parcial |
| Itens não comprados distinguíveis | sem teste — **ACHADO-047** | Gap |
| Itens avulsos identificados pelo nome | dado testado `use-detalhe-compra.test.ts:56`; UI sem teste | Parcial |
| Detalhe numa única consulta com junção externa | `sqlite-compra.repository.test.ts:128,633` | Coberto |
| Produto removido não quebra a tela | query testada `sqlite-compra.repository.test.ts:662`; texto "Produto removido" na UI sem teste | Parcial |
| Histórico é só leitura | ausência de ações de escrita; sem teste que afirme isso — **ACHADO-045** | Gap |
| Paginação em blocos por data (keyset) | `sqlite-compra.repository.test.ts:746`; `use-historico-compras.test.ts:76` (nomeia explicitamente ausência de `deslocamento`) | Coberto |

### `resumo-de-valores` (12 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Valor do estoque = soma qtd×preço, ativos não removidos | `sqlite-produto.repository.test.ts:446`; `use-resumo-valores.test.ts:23` | Coberto |
| Sem preço contribui zero | `sqlite-produto.repository.test.ts:462`; `use-resumo-valores.test.ts:63` | Coberto |
| Removidos excluídos | `sqlite-produto.repository.test.ts:470` | Coberto |
| Conversão pela função única, resultado em centavos | `dinheiro.test.ts:62` | Coberto |
| Valor da lista = soma custos de reposição, qtd arredondada | `estoque.rules.test.ts:56,74,92` | Coberto |
| Nada faltando = zero | `estoque.rules.test.ts:103` | Coberto |
| Valores separados, nunca somados | `use-resumo-valores.test.ts:46` (nomeia "design D2") | Coberto |
| Contagem por estado e sem preço, sinaliza parcial | regra: `use-resumo-valores.test.ts:97,63`; aviso "valor parcial" na tela sem teste — **ACHADO-045** | Parcial |
| Toque em contagem leva à despensa filtrada | sem teste de navegação — **ACHADO-045** | Gap |
| Tipografia dentro do limite, mono+tabular | `tipografia.test.ts:4,20` | Coberto |
| Sem contagem progressiva animada | ausência de código de animação; sem teste explícito | Gap (implícito) |
| Somente leitura, nenhuma escrita ao usar a tela | hook só chama métodos de leitura; sem teste que afirme ausência de escrita (spy) | Gap |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-045 | média | `resumo.tsx`, `historico.tsx`, `historico/[id].tsx` sem teste — estado vazio, distinção de cancelada, aviso de valor parcial, navegação por toque |
| ACHADO-046 | baixa | "Compra finalizada com total zero" sem teste dedicado na agregação mensal |
| ACHADO-047 | baixa | "Itens não comprados distinguíveis" no detalhe da compra sem teste |

**Cobertura**: ~20/31 cenários com teste direto e completo (não contando "N/A"), demais parciais/gaps concentrados — sexta vez consecutiva (PRs 5-11) — na ausência de teste de tela.
