---
pr: 6
branch: change/dar-baixa-caminho-critico
commit: 8460d47
estado: concluida
---

## Foco do checklist

**Caminho crítico**: 1 toque registra sem confirmação/spinner, toques repetidos acumulam serializados, consumo > saldo fixa em 0, desfazer = movimento inverso (nunca DELETE), janela de 10s, desfazer não empilha.

## `npm run verificar`

Verde, exit 0, **sem nenhum warning** — o import não usado de `Milesimos` ([[ACHADO-005]]) e os 2 erros de `set-state-in-effect` ([[ACHADO-006]]) já não aparecem mais nesta PR.

**Achado do plano fechado**: [[ACHADO-006]] (crítico, PR 5) foi corrigido aqui via `eslint-disable-next-line react-hooks/set-state-in-effect` justificado em comentário — falso positivo da regra (o `recarregar` só escreve estado depois do `await`, nunca sincronamente). Atualizado para `estado: descartado`.

## `npm test`

```
Test Suites: 26 passed, 26 total
Tests:       314 passed, 314 total
Time:        8.89 s
```

2 suítes emitem warnings de `act()` no console (não falham o teste) — ver seção "Observação de ferramental" abaixo.

## Verificações específicas

- **1 toque registra sem confirmação/spinner**: `registro-e-desfazer.test.tsx:129` — "toque simples registra sem confirmação e sem navegação" (`StepperConsumo`).
- **Toques repetidos serializados, desfazer não empilha**: `:27` — "três registros em sequência: a confirmação visível aponta o último id"; comentário no próprio teste confirma a intenção: "Substituição, não empilhamento: sobra um registro, o mais recente."
- **Desfaz o movimento certo, não o último gravado**: `:109` — `ToastDesfazer` "desfaz o movimento descrito na confirmação, não o último gravado" — importante porque, com toques rápidos, o último *gravado* no banco pode não ser o último *anunciado* na UI.
- **Vocabulário do usuário**: `:48` — "usa o vocabulário do usuário, sem termo de sistema" (bate com a proibição do CLAUDE.md a "dar baixa"/"movimento de estoque" na UI).
- **Item zerado sem desfazer**: `:79` — "item já zerado avisa sem oferecer desfazer".
- **Falha de gravação**: `:94` — "falha de gravação avisa e guarda a tentativa para repetir".
- **Consumo > saldo fixa em 0 / desfazer = movimento inverso**: já cobertos no domínio desde a PR 2 (`movimento.rules.test.ts`), reutilizados aqui pela camada de aplicação/apresentação.
- **Janela de 10s (KPI K4)**: não há teste unitário para isso — é medição de uso real em emulador/aparelho, prevista para a F6. Ausência aqui não é lacuna.

## Observação de ferramental

`registro-e-desfazer.test.tsx` emite 2x "You seem to have overlapping act() calls" no console durante a suíte de três-registros-em-sequência. Não falha o teste, mas indica que o teste dispara `act()` async sem aguardar o anterior terminar antes do próximo — típico de loop `for...of` com `act(async () => ...)` sem `await` suficiente entre iterações. Registrado como candidato a limpeza, não achado formal (não afeta corretude, só ruído de console) — revisar em F3 se aparecer em mais suítes.

## Achados

- [[ACHADO-006]] fechado (`descartado`) — corrigido nesta PR.

## F4 — Smoke no emulador

Ambiente reaproveitado das rodadas anteriores: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro já ativos. PR-06 é o caminho crítico — já existe flow dedicado (`.maestro/dar-baixa-caminho-critico.yaml`), então priorizei ele sobre o smoke-launch genérico.

- **Efeito colateral real, intencional**: este flow decrementa 1 pacote de Arroz no banco do dispositivo de QA (documentado no próprio YAML).
- **1 toque registra sem confirmação/spinner**: confirmado por interação real — toque em "Registrar consumo de 1 pacote de Arroz" e o item mudou de "1 de 2 pacotes, Falta 1" para "0 de 2 pacotes, Acabou" sem qualquer diálogo de confirmação interposto. Contador do filtro "Acabou" foi de 37→38 na mesma tela, confirmando reatividade em tempo real. Evidência: `qa/por-pr/evidencias/PR-06/smoke-consumo-registrado.png`.
- **Toast "Anotado"**: o `assertVisible: "Anotado"` do flow original falhou por timing — o toast aparece e some rápido demais para a janela padrão de assert do Maestro nesta execução (o app já tinha um estado de navegação residual da rodada anterior, exigindo um `launchApp` extra antes de estabilizar). A mutação de estado (quantidade e contadores) confirma que a ação central funcionou; a falha foi de sincronismo da asserção, não do app. Não abro achado novo — já existe cobertura de unidade para o vocabulário do toast (`registro-e-desfazer.test.tsx:48`).
- **Verificação de display**: `topResumedActivity=com.danielfalcaodf.repor/.MainActivity`, display único.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes`, não um checkout isolado do commit `8460d47`/`change/dar-baixa-caminho-critico`.

**PR-06 concluída.**

## F5 — Fluxo E2E

Flow: `.maestro/reposicao-manual.yaml`. Rodada de extensão da F5 (gap identificado em verificação posterior aos 5 flows originais), cobrindo o caminho **"Repus"** (botão "+" de reposição rápida, `BotaoReporRapido`) que nunca teve E2E nem teste unitário positivo ([[ACHADO-027]]: `onRepus` só tinha teste negativo). Ambiente: `emulator-5554`.

**Resultado: passou.** Roteiro: Despensa → item "Macarrão" (`0 de 2 pacotes, Acabou`) → toque em "Repor 1 pacote de Macarrão" → toast "Anotado: repus 1 pacote de Macarrão" → item passa a `1 de 2 pacotes, Falta 1`.

- Trocado de "Arroz" (escolha inicial) para "Macarrão" porque Arroz já estava `Cheio` (`3 de 2 pacotes`) por causa de execuções anteriores de `dar-baixa-caminho-critico.yaml` na campanha — repor mais violaria a instrução de evitar item já cheio.
- **Nota de vocabulário**: o rótulo textual do item após a ação não é "Faltando" (nome do filtro/tab da lista), é **"Falta 1"** (rótulo do item com quantidade concatenada) — divergência de nomenclatura entre tela e componente, não um bug, só uma armadilha de seletor corrigida durante a autoria.
- **Efeito colateral real e permanente**: Macarrão ficou em `1 de 2 pacotes` no dispositivo de QA.

Nenhum achado de comportamento novo (as duas observações acima são notas de processo, não divergências de spec).

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-dar-baixa-caminho-critico/`. Quatro capabilities auditadas (~50 cenários), incluindo um delta `MODIFIED` de `medidor-linha-dagua` (animação condicional). `qa-expert` seguiu indisponível como `subagent_type`; auditoria conduzida diretamente. **Correção a uma nota da própria PR-06.md**: a seção F2 tratou "janela de 10s" como sinônimo do KPI K4 (tempo de tarefa) e descartou como "medição de F6" — são coisas diferentes. A janela de desfazer de 10s é um *timer de UI* (`JANELA_DESFAZER` em `toast-desfazer.tsx`), testável em Jest; o KPI K4 (tempo de tarefa do usuário) é que depende de F6.

### Capability: `registro-de-consumo`

| Cenário (agrupado por requisito) | Teste | Status |
|---|---|---|
| Toque único registra e salva / sem confirmação / sem indicador de carregamento | `registro-e-desfazer.test.tsx:129` | coberto |
| Toques repetidos acumulam | `registro-e-desfazer.test.tsx:27` (três registros em sequência) | coberto |
| Meta do caminho crítico (≤3 toques, ≤10s) | é o KPI K4 — dependente de medição real em F6, não é lacuna desta fase | descoberto (dependente de F6, já rastreado no plano) |
| Gravação conjunta / falha não altera nada / registro com autor e resultado | já cobertos na camada de repositório (PR-03, `sqlite-produto.repository.test.ts:146,177`) e reafirmados aqui via `registro-e-desfazer.test.tsx:94` ("falha de gravação avisa e guarda a tentativa") | coberto |
| Consumo maior que o saldo fixa em zero / aviso ao zerar / item já zerado não registra | domínio (PR-02, `movimento.rules.test.ts`) + `registro-e-desfazer.test.tsx:64,79` (aviso ao zerar, item zerado avisa sem desfazer) | coberto |
| Painel abre com toque longo | `registro-e-desfazer.test.tsx:149` | coberto |
| Campo único sem formulário / registrar consumo específico / decimal em unidade divisível | `registro-e-desfazer.test.tsx:191` (`TecladoQuantidade`, consumo com `'1,5'`) | coberto |
| **Registrar reposição específica** | `TecladoQuantidade` tem `onRepus` (`teclado-quantidade.tsx:18,101`), mas o único teste que o menciona é negativo ("fechar sem confirmar não altera nada", linha 202-207) — nenhum teste confirma `onRepus` chamado com a quantidade correta ao escolher "Repus" | **descoberto** |
| Fechamento sem registrar | `registro-e-desfazer.test.tsx:200` | coberto |
| Ações no detalhe visíveis / equivalência de efeito | tela de detalhe (`app/produto/[id].tsx`) sem teste — mesmo padrão de telas sem cobertura já identificado na PR-05 | descoberto (mesmo padrão já rastreado, não duplicado) |
| Nomes coerentes / ausência de jargão | `registro-e-desfazer.test.tsx:48` | coberto |
| Funciona offline | `offline.test.ts:27` (PR-03, nenhuma API de rede em todo o código-fonte) | coberto |

### Capability: `desfazer-registro`

| Cenário (agrupado) | Teste | Status |
|---|---|---|
| Ação disponível por 10s / tempo restante visível / expiração encerra / confirmação não bloqueia | `toast-desfazer.tsx:3` declara `JANELA_DESFAZER = 10_000` passado para `Toast` (`duracao`), mas `toast.tsx` não tem teste próprio — mesma lacuna já registrada em [[ACHADO-016]] (PR-04). Os 4 cenários desta seção dependem diretamente dessa mesma lacuna, não duplicada aqui. | descoberto (já rastreado via ACHADO-016) |
| Movimento inverso gravado / trilha preservada / atômico / desfazer de consumo que zerou | já cobertos no domínio (PR-02, `movimento.rules.test.ts`) e no repositório (PR-03, `sqlite-movimento.repository.test.ts:69`) | coberto |
| Registros sucessivos (não empilha) / desfazer aponta para o exibido | `registro-e-desfazer.test.tsx:27,109` | coberto |
| Texto descritivo / vocabulário do usuário | `registro-e-desfazer.test.tsx:48` | coberto |

### Capability: `movimento-do-gesto`

| Cenário (agrupado) | Teste | Status |
|---|---|---|
| Retorno tátil imediato / contração 0,92 / nível desce com mola / números trocam sem deslizar / nenhum elemento se desloca | `stepper-consumo.tsx` chama `Haptics.impactAsync` e `withTiming` para a contração (linhas 47-50); `medidor-nivel.tsx` usa `molar()` (spring). **Nenhum teste verifica que `Haptics.impactAsync` é chamado, nem a sequência/timing da coreografia** — `registro-e-desfazer.test.tsx:129` só confirma que o callback de registro é chamado, não a orquestração visual | **descoberto** |
| Animação na thread de interface / sem trabalho de animação em JS | `medidor-nivel.tsx:37,40` usa `useSharedValue`/`useAnimatedStyle` do Reanimated (roda na UI thread por construção da biblioteca) — verificável por inspeção de código, sem teste de performance dedicado (correto, não é testável em Jest) | coberto (por construção da biblioteca) |
| Nada mais anima (abertura de tela, rolagem, contagem progressiva) | consistente com a ausência de teste de animação já registrada em [[ACHADO-019]] (PR-04) | descoberto (mesma classe, não duplicado) |
| Redução de movimento: corte seco / retorno tátil preservado / informação não depende do movimento | mesma lacuna do [[ACHADO-019]] (PR-04) — não duplicado; "informação não depende do movimento" decorre dos 3 canais redundantes já testados em `medidor-e-item.test.tsx` (PR-05) | descoberto (parcial, já rastreado) / coberto (canais redundantes) |
| Gravação não espera animação / saída de tela durante animação não quebra | a gravação em `use-registro-de-consumo.ts` não depende de nenhuma promise de animação (chamada síncrona ao resultado do caso de uso) — por inspeção, atende ao requisito; sem teste que simule desmontagem do componente durante uma animação em andamento | coberto (por inspeção) / descoberto (teste de desmontagem durante animação) |

### Capability: `medidor-linha-dagua` (delta — animação condicional)

| Cenário (agrupado) | Teste | Status |
|---|---|---|
| Rolagem não anima / sem entrada em cascata | mesma lacuna já registrada (ACHADO-019, PR-04) | descoberto (já rastreado) |
| Mudança de quantidade anima com mola | `medidor-nivel.tsx:40` — `nivel.set(animar ? molar(fracao) : fracao)` implementa a condicional, mas nenhum teste renderiza com `animar={true}` e verifica a chamada a `molar()` | **descoberto** |
| Apenas o item alterado anima | a prop `animar` por item é exatamente o mecanismo que garante isso — nenhum teste renderiza uma lista com dois itens, muda um, e confirma que só aquele recebeu `animar={true}` | **descoberto (mesma causa-raiz do item acima)** |
| Reordenação após mudança de estado não anima o nível | nenhum teste cobre o cenário de reordenação da lista após troca de estado | **descoberto** |

### Achados desta rodada

- Ver `qa/achados/ACHADO-026.md` — coreografia do gesto (`Haptics.impactAsync`, timing da contração/mola) sem nenhum teste que verifique a orquestração, só o efeito final (callback chamado).
- Ver `qa/achados/ACHADO-027.md` — `TecladoQuantidade`: caminho de "Repus" (reposição específica) sem teste positivo, só o caminho de "Usei" é testado com valor.
- Ver `qa/achados/ACHADO-028.md` — prop `animar` de `medidor-nivel.tsx` (que decide se anima com mola ou pinta direto) sem nenhum teste, incluindo o cenário central da capability "apenas o item alterado anima".
- [[ACHADO-016]] (PR-04, `toast.tsx` sem teste) e [[ACHADO-019]] (PR-04, `reduceMotion` sem teste) reafirmados como causa-raiz de várias lacunas desta PR — não duplicados.

**Resumo**: cobertura muito forte no caminho crítico de dados (registrar, desfazer, mensagens, atomicidade — tudo já testado, incluindo reaproveitamento de PRs anteriores). As lacunas concentram-se consistentemente na camada de coreografia visual/animação, que seguiu sem receber testes de comportamento (só testes de efeito colateral de dados) em toda a cadeia até aqui.
