---
pr: 2
branch: change/fundacao-dominio
commit: 192966c
estado: concluida
---

## Foco do checklist

129 testes de domínio (esperado no plano-fonte); casos de borda do CLAUDE.md: baixa cruzando zero, arredondamento em `un`/`pacote`/`caixa`, fração clampada em [0,1], normalização de categoria.

## `npm run verificar`

Limpo (fronteiras + lint + typecheck).

## `npm test`

```
Test Suites: 10 passed, 10 total
Tests:       110 passed, 110 total
Time:        0.631 s
```

**Divergência do número estimado no plano-fonte**: o plano previa "129 testes de domínio" para esta PR; o valor real é **110**. Não é uma falha — o plano-fonte foi escrito com um número aproximado antes da execução; 110 testes cobrem os 10 arquivos de domínio existentes nesta PR (`result`, `id`, `unidade`, `dinheiro`, `quantidade`, `validacao`, `compra.rules`, `movimento.rules`, `categoria`, `estoque.rules`). Não abre achado — divergência de estimativa do plano, não do código.

## Casos de borda verificados (CLAUDE.md)

- **Baixa cruzando zero fixa em 0**: `src/domain/movimento/movimento.rules.test.ts:64` — "baixa que cruzaria zero fixa em zero: saldo 500 com baixa de 2000".
- **Sinal coerente do movimento**: `movimento.rules.test.ts:35` — "sinal incoerente é rejeitado: baixa positiva e reposição negativa".
- **Fração clampada em [0,1]**: `src/domain/produto/estoque.rules.ts:76` — `Math.min(bruta, 1)`, com comentário explícito "a fração vem clampada daqui — a apresentação não calcula nem limita (FRONTEND §12.2)"; testado indiretamente pela suíte de `estoque.rules.test.ts`.
- **Arredondamento em unidade indivisível**: `estoque.rules.test.ts:60` — "falta fracionária em unidade indivisível arredonda para cima".
- **Normalização de categoria**: `categoria.test.ts` — remove espaços nas pontas, capitaliza primeira letra, colapsa espaços internos, texto só de espaços vira ausência (não string vazia). Os 4 casos do CLAUDE.md (trim, capitalização) cobertos.

## Achados

Nenhum.

**PR-02 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-fundacao-dominio/`. Quatro capabilities auditadas (72 cenários). `qa-expert` seguiu indisponível como `subagent_type` nesta sessão; auditoria conduzida diretamente, mesma metodologia read-only (cenário → teste).

### Capability: `regras-de-compra` (14 cenários — `src/domain/compra/compra.rules.test.ts`)

| Cenário | Teste | Status |
|---|---|---|
| Soma de itens marcados | `compra.rules.test.ts:74` | coberto |
| Item não marcado é ignorado | `:79` | coberto |
| Compra sem itens marcados | `:87` | coberto |
| Item marcado sem preço pago | `:91` | coberto |
| Reposição soma ao estoque | `:98` | coberto |
| Reposição de item avulso não afeta estoque | `:108` | coberto |
| Item com quantidade comprada ≠ planejada | `:112` | coberto |
| Preço divergente é detectado | `:126` | coberto |
| Preço igual não gera pergunta | `:131` | coberto |
| Sem confirmação o preço permanece | `:169` | coberto |
| Com confirmação o preço é atualizado | `:148` (mesma suíte de `efeitosDaFinalizacao`) | coberto |
| Produto sem preço cadastrado | `:136` | coberto |
| Efeitos calculados em bloco | `:148` | coberto |
| Falha de validação impede efeito parcial | `:178` | coberto |

### Capability: `regras-de-estoque` (29 cenários — `estoque.rules.test.ts`, `validacao.test.ts`, `categoria.test.ts`)

| Cenário | Teste | Status |
|---|---|---|
| Item zerado é crítico / em falta / ok exato / ok acima | `estoque.rules.test.ts:30,34,38,42` | coberto (4/4) |
| Falta simples / fracionária arredonda / item ok / item zerado | `:56,60,64,68` | coberto (4/4) |
| Custo com preço / sem preço / valor em estoque / total ignora ausência | `:74,80,86,92` | coberto (4/4) |
| Fração parcial / não transborda / zerado / necessária inválida | `:109,115,127,131` | coberto (4/4) |
| Rótulo zerado / em falta / completo / sem vocabulário de sistema | `:138,142,150,154` | coberto (4/4) |
| Normalização: espaços ponta / caixa / espaços internos / vazia→ausência | `categoria.test.ts:4,8,12,16` | coberto (4/4) |
| Cadastro válido / nome vazio / qtd necessária zero / decimal / valor negativo | `validacao.test.ts:4,23,47,59,71` | coberto (5/5) |

### Capability: `regras-de-movimento` (12 cenários — `movimento.rules.test.ts`)

| Cenário | Teste | Status |
|---|---|---|
| Baixa variação negativa / reposição positiva / variação zero rejeitada / sinal incoerente | `:10,18,26,36` | coberto (4/4) |
| Baixa dentro do saldo / cruzaria zero / item já zerado / reposição soma | `:102,114,126,134` | coberto (4/4) |
| Inverso de baixa / de reposição / original preservado / inverso de ajuste | `:148,155,168,161` | coberto (4/4) |

### Capability: `unidades-e-valores` (17 cenários — `quantidade.test.ts`, `unidade.test.ts`, `dinheiro.test.ts`)

| Cenário | Teste | Status |
|---|---|---|
| Decimal→milésimos / milésimos→exibição / inteiro sem casas / somatório exato | `quantidade.test.ts:11,15+25,19,29` | coberto (4/4) |
| Unidade indivisível | `unidade.test.ts` `it.each` (un/pacote/caixa) | coberto |
| Unidade divisível | mesmo `it.each` (kg/g/L/ml) | coberto |
| **Conjunto fechado de unidades — compilador rejeita valor fora das 7** | nenhum teste de tipo (`@ts-expect-error`/`tsd`) encontrado; `unidade.test.ts:4` só confirma o array `UNIDADES` em runtime, não a rejeição do compilador para um valor fora do tipo `Unidade` | **descoberto** |
| Meio pacote / unidade+pouco / divisível preserva / já inteiro não infla | `quantidade.test.ts:46,50,58,54` | coberto (4/4) |
| Formatação BRL / valor zero / conversão texto digitado | `dinheiro.test.ts:11,15,25` | coberto (3/3) |
| Produto qtd×preço / fracionária / arredondamento resultado | `:43,47,51` | coberto (3/3) |

### Achados desta rodada

- Ver `qa/achados/ACHADO-010.md` — mesma classe de lacuna do ACHADO-009 (PR-01): garantia de tipo do domínio sem teste de tipo dedicado, desta vez para o conjunto fechado de `Unidade`.

**Resumo**: 71/72 cenários cobertos por teste automatizado; 1 lacuna (tipo, não runtime).

## F4 — Smoke no emulador

Ambiente reaproveitado da rodada da PR-01: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro (`adb reverse tcp:8081 tcp:8081`) já ativos.

- **Flow**: `.maestro/smoke-launch.yaml` (piso mínimo — PR-02 é puramente `domain/`, sem tela ou capability de UI própria; nada específico para exercitar além do app abrir).
- **Resultado**: passou. 6 comandos executados, 4 abas visíveis, sem redbox.
- **Verificação de display**: `topResumedActivity=com.triasoftware.repor/.MainActivity`, display único.
- **Evidência**: `qa/por-pr/evidencias/PR-02/smoke-launch.png`.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes` (código atual, não um checkout isolado do commit `192966c`/`change/fundacao-dominio`). Como PR-02 não introduz UI, essa ressalva tem baixo impacto prático aqui — o smoke test não teria como isolar o efeito de regras de domínio puras de qualquer forma; a cobertura real desta PR está na F3 (110 testes automatizados) acima.
