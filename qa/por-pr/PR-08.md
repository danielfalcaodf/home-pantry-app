---
pr: 8
branch: feature/modo-compra-e-fechamento
commit: 1d28124
estado: concluida
---

## Foco do checklist

Fechamento atômico com falha injetada no meio (rollback total), itens não marcados voltam à lista, atualização de preço só com confirmação, avulso não repõe.

## `npm run verificar`

Verde, exit 0, sem warnings.

## `npm test`

```
Test Suites: 40 passed, 40 total
Tests:       405 passed, 405 total
Time:        8.619 s
```

Mesmos 2 warnings de `act()` sobrepostos, carregados desde a PR 6.

## Verificações específicas

- **Fechamento atômico / rollback em falha**: `use-finalizar-compra.test.ts:148` — "falha na gravação preserva as marcações, sem alterar a situação da compra" — a falha no meio do fechamento não deixa estado parcial.
- **Itens não marcados não geram reposição** (ficam implicitamente na lista, já que a lista é derivada): `:112`.
- **Avulso não repõe estoque**: `:93` — "avulso marcado não altera nenhuma quantidade nem gera reposição" — confirma que o avulso é só item de compra, nunca produto existente.
- **Atualização de preço só com confirmação (tri-state)**: `:171,186,201,216` — confirmação atualiza, recusa não atualiza, ausência de resposta não atualiza, produto sem preço registra o primeiro preço na confirmação.
- **Falha no fechamento não altera preços já confirmados**: `:231` — garante que a atualização de preço também é atômica com o fechamento, não uma operação solta.
- **Total do rodapé bate com o total gravado**: `:48` — evita divergência entre o que o usuário vê antes de fechar e o que fica persistido.
- **Cancelamento de compra aberta**: `use-cancelar-compra.test.ts:14,31` — cancela compra existente, falha explícita quando a compra já não existe mais.

## Achados

Nenhum novo.

**PR-08 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-03-modo-compra-e-fechamento`. 4 capabilities, ~45 cenários mapeados contra a implementação e os testes.

### `atualizacao-de-preco-referencia` (10 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Pergunta ao divergir | `compra.rules.test.ts:126`; `item-compra.test.tsx:93` | Coberto |
| Sem pergunta quando igual | `compra.rules.test.ts:131` | Coberto |
| Produto sem preço cadastrado oferece registrar | `compra.rules.test.ts:136`; `use-modo-compra.test.ts:127` | Coberto |
| Pergunta não bloqueia modo compra | `item-compra.tsx:117-133` (inline, sem `Modal`); `item-compra.test.tsx:93` | Coberto |
| Confirmação atualiza | `use-finalizar-compra.test.ts:171` | Coberto |
| Recusa preserva | `use-finalizar-compra.test.ts:186` | Coberto |
| Sem resposta preserva | `use-finalizar-compra.test.ts:201` | Coberto |
| Atualização só no fechamento (não antes) | `use-modo-compra.test.ts:80` | Coberto |
| Atualização é parte da transação (falha não altera preço) | `use-finalizar-compra.test.ts:231` (nível de hook); repositório real (`sqlite-compra.repository.test.ts:263`) sabota só reposição, não `atualizacoesDePreco` | Parcial |
| Item avulso não gera pergunta | `use-modo-compra.test.ts:129`; `item-compra.test.tsx:76` | Coberto |

### `fechamento-de-compra` (16 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Fechamento atômico | `sqlite-compra.repository.test.ts:202` | Coberto |
| Falha no meio não aplica nada | `sqlite-compra.repository.test.ts:263` (sabotagem com `produto-fantasma`) | Coberto |
| Estado parcial impossível | mesmo teste acima | Coberto |
| Erro informa e permite repetir | hook: `use-finalizar-compra.test.ts:148`; tela (`app/compra/[id].tsx`) sem teste — **ACHADO-035** | Parcial |
| Reposição soma ao estoque | `compra.rules.test.ts:98`; `sqlite-compra.repository.test.ts:247` | Coberto |
| Movimento vinculado à compra | `sqlite-compra.repository.test.ts:254` (conta por `compra_id`, não verifica campos individuais) | Parcial |
| Movimento registra autor/data/variação/resultado | implementação em `sqlite-compra.repository.ts:271-283`; sem teste de valores individuais — **ACHADO-036** | Gap |
| Item avulso não repõe estoque | `compra.rules.test.ts:108`; `sqlite-compra.repository.test.ts:327` | Coberto |
| Não marcados ignorados na reposição | `use-finalizar-compra.test.ts:112` | Coberto |
| Pendentes voltam à lista | `sqlite-compra.repository.test.ts:502` | Coberto |
| Total calculado no fechamento | `compra.rules.test.ts:74`; `use-finalizar-compra.test.ts:47` | Coberto |
| Itens sem preço pago não invalidam total | `compra.rules.test.ts:91` | Coberto |
| Situação finalizada exige data | `migrations.test.ts:140`; `use-finalizar-compra.test.ts:135` | Coberto |
| Confirmação em linguagem do usuário | implementação `app/compra/[id].tsx:42-56`; sem teste de tela — **ACHADO-035** | Gap |
| Retorno à despensa com estado atualizado | implementação `app/compra/[id].tsx:145-156`; sem teste — **ACHADO-035** | Gap |
| Fechamento funciona offline | garantido pela arquitetura (sem I/O de rede no caminho); sem teste nomeado | Gap (implícito) |

### `lista-derivada` (6 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Materialização no início da compra | `use-iniciar-compra.test.ts:33` | Coberto |
| Lista permanece derivada antes disso | `use-lista-compras.test.ts:102` | Coberto |
| Quantidade planejada preserva arredondamento | `use-iniciar-compra.test.ts:47` | Coberto |
| Preço estimado registrado no item | `use-iniciar-compra.test.ts:48` | Coberto |
| Avulsos já pertencem à compra, não duplicados | `use-iniciar-compra.test.ts:52` | Coberto |
| Alterações de estoque após iniciar não mudam itens planejados | `sqlite-compra.repository.test.ts:424` | Coberto |

### `modo-compra` (18 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Sem subtelas | implementação sem `router.push` em `app/compra/[id].tsx`; sem teste de tela — **ACHADO-035** | Gap |
| Ajuste sem sair da tela (sheet sobreposto) | `sheet-ajuste-compra.test.tsx:12` (isolado, não integração) | Parcial |
| Sair exige intenção | **implementação divergente do spec** — aviso é texto estático sempre visível, não condicionado ao botão voltar nem a itens marcados (`app/compra/[id].tsx:96-98`, `botao-voltar.tsx:13`) — **ACHADO-034** | Gap / divergência |
| Tela permanece acesa | `useKeepAwake()` em `app/compra/[id].tsx:28`; sem teste — **ACHADO-035** | Gap |
| Comportamento normal restaurado ao sair | idem — sem teste — **ACHADO-035** | Gap |
| Item marcado muda de aparência | `item-compra.tsx:76-98`; teste só verifica callback, não estilo — **ACHADO-037** | Parcial |
| Desmarcar reverte | `use-modo-compra.test.ts:46` | Coberto |
| Alvo de toque adequado (≥48×48) | `item-compra.tsx:59-88` (`ALVO_TOQUE_MINIMO`); sem asserção de geometria no teste — **ACHADO-037** | Parcial |
| Controle quadrado, não circular | `raio.linha = 0` (`espaco.ts:14`); sem asserção no teste — **ACHADO-037** | Parcial |
| Padrão assume o planejado | `use-modo-compra.test.ts:33` | Coberto |
| Quantidade ajustada | `use-modo-compra.test.ts:66` | Coberto |
| Preço pago informado | `use-modo-compra.test.ts:66` | Coberto |
| Preço pago ausente não impede fechamento | `compra.rules.test.ts:91` (via domínio) | Coberto |
| Item marcado exige quantidade (banco rejeita) | `migrations.test.ts:186` | Coberto |
| Contador atualiza a cada marcação | `rodape-compra.test.tsx:37`; integração com a tela sem teste — **ACHADO-035** | Parcial |
| Total corrente atualiza | `rodape-compra.test.tsx:37` | Coberto |
| Total sem contagem progressiva | ausência de `Animated` no componente; sem teste dedicado | Gap (implícito) |
| Números não deslocam layout (tabular-nums) | `tipografia.test.ts:20` | Coberto |
| Conversão dos itens ao iniciar (avulsos incluídos, qtd arredondada) | `use-iniciar-compra.test.ts:33,52` | Coberto |
| Retomar compra em andamento preserva marcações | `use-modo-compra.test.ts:132` | Coberto |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-034 | média | Aviso de saída com compra marcada é texto estático, não responde ao botão voltar nem checa itens marcados — divergente do spec |
| ACHADO-035 | média | `app/compra/[id].tsx` (tela do modo compra) sem nenhum teste — deixa sem cobertura navegação, keep-awake, rodapé, mensagem de fechamento e retorno à despensa |
| ACHADO-036 | baixa | Campos individuais do movimento de estoque (autor/data/variação/resultado) não verificados por teste, só a contagem |
| ACHADO-037 | baixa | Estilo do item marcado (cor/risco) e geometria do controle (quadrado/48×48) não verificados por asserção de estilo |

**Cobertura**: ~30/45 cenários com teste direto, 1 divergência de implementação identificada (ACHADO-034), demais gaps concentrados em ausência de teste de tela (`app/compra/[id].tsx`).
