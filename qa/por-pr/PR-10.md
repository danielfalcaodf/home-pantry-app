---
pr: 10
branch: feature/ajuste-e-conferencia-estoque
commit: 0bf8010
estado: concluida
---

## Foco do checklist

Ajuste sem mudança não grava, motivo opcional aparece no histórico, paginação por data (keyset, não offset), correção em bloco com rollback, conferência retomável.

## `npm run verificar`

Verde, exit 0, sem warnings.

## `npm test`

```
Test Suites: 55 passed, 55 total
Tests:       530 passed, 530 total
Time:        8.983 s
```

## Verificações específicas

- **Ajuste sem mudança não grava**: `use-ajustar-estoque.test.ts:56` — "valor igual ao registrado não grava nada", retorna `{ ajustou: false, motivo: 'sem_mudanca' }`.
- **Motivo opcional**: `:33` — motivo default `null`; `:73` — "motivo é passado ao repositório quando informado" (`'vencimento'`).
- **Valor negativo rejeitado sem side-effect**: `:64` — rejeitado sem chamar o repositório.
- **Paginação keyset (não offset)**: `use-historico.test.ts:74` — "carregarMais continua a partir da data do último item, **não de um deslocamento**" — nomeia explicitamente a técnica keyset vs. offset; `:65` carrega um bloco por vez em histórico grande; `:89,97` sinaliza fim de página corretamente.
- **Conferência retomável**: `use-conferencia.test.ts:112` — "retoma de onde parou: posição salva reabre direto no percurso"; `:124` — "percurso concluído limpa a posição salva".
- **Confirmar sem gravar movimento**: `:59` — "confirmar avança sem gravar nenhum movimento" (item já correto na conferência não gera ruído no histórico).
- **Corrigir grava e avança**: `:73` — cada correção no percurso é um ajuste individual, não uma transação em lote.

## Observação sobre "correção em bloco com rollback"

Não há teste de rollback de **múltiplas** correções em uma única transação — e isso é coerente com o design, não uma lacuna: a conferência é explicitamente **retomável** (`:112,124`), o que exige que cada correção individual seja commitada de forma independente (senão "retomar de onde parou" não faria sentido — um rollback em bloco desfaria progresso já salvo). O item do checklist do plano-fonte provavelmente se referia ao ajuste individual ser atômico (gravar-ou-não, nunca parcial), que já está coberto por `use-ajustar-estoque.test.ts:87` ("falha de gravação é sinalizada sem alterar a quantidade"). Não abre achado — ajuste de entendimento do plano, não do código.

## Achados

Nenhum novo.

**PR-10 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-03-ajuste-e-conferencia-estoque`. 5 capabilities, ~45 cenários mapeados. Nota: a observação da F2 acima sobre "correção em bloco com rollback" da **conferência** (por item, não em lote) é distinta da capability `diagnostico-de-integridade`, que tem seu próprio requisito de correção em bloco numa única transação (`corrigirTodasDivergencias`, testado e coberto) — não são o mesmo requisito.

### `ajuste-de-estoque` (15 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Ajuste para cima/baixo/zero | `movimento.rules.test.ts:53,61,69`; `sqlite-produto.repository.test.ts:255,287,308`; `use-ajustar-estoque.test.ts:24,37,47` | Coberto |
| Ajuste sem mudança não grava | `movimento.rules.test.ts:77`; `use-ajustar-estoque.test.ts:56` | Coberto |
| Atomicidade | `sqlite-produto.repository.test.ts:378`; `use-ajustar-estoque.test.ts:87` | Coberto |
| Quantidade negativa rejeitada | `movimento.rules.test.ts:85`; `use-ajustar-estoque.test.ts:64`; `sheet-ajuste-estoque.test.tsx:73` | Coberto |
| Motivo registrado / opcional / visível no histórico | `sqlite-produto.repository.test.ts:347`; motivo no histórico: implementação em `historico.tsx:62-66` sem teste de tela — **ACHADO-042** | Parcial |
| Tipo preservado como ajuste | `movimento.rules.test.ts:93` | Coberto |
| Distinção no histórico (cor) | implementação `cor-do-estado.ts:16-24`; sem teste — **ACHADO-043** | Gap |
| Vocabulário de correção | `historico-do-movimento.test.ts:29,37` | Coberto |
| Edição abre o ajuste (toque na quantidade) | implementação `app/produto/[id].tsx:185-194`; sem teste de tela — **ACHADO-041** | Gap |
| Efeito idêntico a qualquer caminho | reuso do mesmo hook (`use-ajustar-estoque`), coberto indiretamente; composição na tela sem teste — **ACHADO-041** | Parcial |
| Quantidade necessária é edição de cadastro, sem movimento | `hooks.test.ts:130` | Coberto |

### `cadastro-de-produto` delta (7 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Quantidade em destaque (papel display) | implementação `app/produto/[id].tsx:180-194`; sem teste — **ACHADO-041** | Gap |
| Edição persistida, reflete na despensa | `sqlite-produto.repository.test.ts:64`; `hooks.test.ts:130` | Coberto |
| Toque na quantidade abre ajuste | sem teste — **ACHADO-041** | Gap |
| Quantidade atual não é campo de formulário comum | implementação `formulario-produto.tsx:45,59,146-151` (`quantidadeAtualEditavel`); sem teste — **ACHADO-041** | Gap |
| Resumo do histórico recente | `use-resumo-historico.test.ts:40` (lógica); texto na tela sem teste | Parcial |
| Acesso ao histórico completo | sem teste — **ACHADO-041** | Gap |
| Aumentar quantidade necessária muda estado sem movimento | `estoque.rules.test.ts:29`; `hooks.test.ts:130` | Coberto |

### `diagnostico-de-integridade` (10 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Verificação compara materializado vs soma dos movimentos | `sqlite-movimento.repository.test.ts:102`; `use-diagnostico.test.ts:22` | Coberto |
| Banco íntegro informa "nenhuma divergência" | lógica coberta (`:102`); mensagem de UI sem teste | Parcial |
| Divergência lista valor registrado e calculado | `sqlite-movimento.repository.test.ts:108`; `use-diagnostico.test.ts:22` | Coberto |
| Verificação não escreve nada | implementação só-leitura; sem teste explícito — **ACHADO-044** | Gap |
| Acessível pelas configurações | `configuracoes.tsx:133-139`; sem teste — já coberto por **ACHADO-038** (PR-09) | Gap (achado existente) |
| Correção individual grava ajuste | `sqlite-movimento.repository.test.ts:143`; `use-diagnostico.test.ts:40` | Coberto |
| Correção em bloco, uma transação | `sqlite-movimento.repository.test.ts:208,227`; `use-diagnostico.test.ts:69` | Coberto |
| Verificação limpa após corrigir | `sqlite-movimento.repository.test.ts:176,213` | Coberto |
| Correção é opcional | implícito (sem ação automática); sem teste explícito — **ACHADO-042** | Parcial |
| Mensagens sem alarme, afirmativa/acionável | implementação `app/diagnostico.tsx:36-79`; sem teste — **ACHADO-042** | Gap |

### `historico-do-produto` (11 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Ordem cronológica inversa | `sqlite-movimento.repository.test.ts:47` | Coberto |
| Conteúdo de cada linha | `historico-do-movimento.test.ts:5-46` (lógica); linha completa na tela sem teste | Parcial |
| Tipos distinguíveis (cor) | sem teste — **ACHADO-043** | Gap |
| Reposição vinculada a compra indicada | `historico-do-movimento.test.ts:21` | Coberto |
| Números em família monoespaçada | decisão visual, não testável por lógica pura | N/A |
| Consumo com o mesmo verbo ("Usei") | `historico-do-movimento.test.ts:5` | Coberto |
| Ausência de jargão | `historico-do-movimento.test.ts:37` | Coberto |
| Histórico é só leitura | ausência de funcionalidade, sem teste dedicado — **ACHADO-042** | Gap |
| Correção pelo caminho de ajuste | reforçado por design; sem teste direto | Gap |
| Carga inicial limitada (bloco) | `use-historico.test.ts:65` | Coberto |
| Continuação por data (keyset, não offset) | `use-historico.test.ts:74` | Coberto |

### `modo-conferencia` (14 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Escolha da categoria / "conferir tudo" | `use-conferencia.test.ts:35,51` | Coberto |
| Um item por vez, quantidade em destaque | `use-conferencia.test.ts:41` (lógica); destaque visual sem teste — **ACHADO-042** | Parcial |
| Progresso visível (N de M) | implementação `app/conferencia.tsx:97-99`; sem teste — **ACHADO-042** | Gap |
| Confirmar avança sem gravar movimento | `use-conferencia.test.ts:59` | Coberto |
| Corrigir grava ajuste e avança | `use-conferencia.test.ts:73` | Coberto |
| Interrupção preserva o feito | coberto indiretamente pelo teste de retomada; sem teste explícito de "sair no meio" | Parcial |
| Retomada a partir de onde parou | `use-conferencia.test.ts:112` | Coberto |
| Conclusão informa corrigidos/corretos | `use-conferencia.test.ts:87` | Coberto |
| Não cria nem remove produtos no percurso | `use-conferencia.test.ts:103` | Coberto |
| Item zerado também é conferido | `use-conferencia.test.ts:41` | Coberto |
| Confirmar exige um único toque | implementação `app/conferencia.tsx:108`; sem teste — **ACHADO-042** | Gap |
| Correção sem diálogo adicional | lógica coberta (`use-conferencia.test.ts:73`); ausência de `Alert` na tela sem teste — **ACHADO-042** | Parcial |
| Funciona offline | garantido por ausência estrutural de I/O de rede; sem teste nomeado | Gap (implícito) |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-041 | média | `app/produto/[id].tsx` sem teste — ajuste via toque na quantidade, campo não editável, resumo do histórico |
| ACHADO-042 | média | `app/conferencia.tsx`, `app/diagnostico.tsx` e `app/produto/[id]/historico.tsx` sem teste — progresso, toque único, ausência de diálogo, mensagens |
| ACHADO-043 | baixa | `cor-do-estado.ts` (distinção visual de tipos de movimento) sem teste |
| ACHADO-044 | baixa | "Verificação não altera dados" do diagnóstico sem teste explícito |

**Cobertura**: ~28/45 cenários com teste direto e completo, ~10 parciais (lógica coberta, tela não), restante concentrado em ausência de teste de tela — mesmo padrão sistemático já visto desde a PR-05.
