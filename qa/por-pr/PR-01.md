---
pr: 1
branch: change/bootstrap-projeto-expo
commit: cb9a365
estado: concluida
---

## Foco do checklist

`verificar:fronteiras` sai 0; hex fora de `tokens.ts` é erro de lint; `Result` e UUID v7; suíte `domain` roda sem emulador.

## `npm run verificar`

```
Fronteiras OK: src/domain/ é TypeScript puro.
expo lint → limpo
tsc --noEmit → limpo
```

## `npm test`

```
Test Suites: 2 passed, 2 total
Tests:       9 passed, 9 total
Time:        0.423 s
```

Só `src/shared/result.test.ts` e `src/shared/id.test.ts` existem nesta PR — é o scaffold inicial (`Result<T,E>` e UUID v7), como esperado.

## Verificações específicas

- **Regra de lint sem hex**: confirmada em `eslint.config.js` — `no-restricted-syntax` com seletor `Literal[value=/^#[0-9a-fA-F]{3,8}$/]`, aplicada a `src/**/*.{ts,tsx}` e `app/**/*.{ts,tsx}`, com `ignores: ['src/presentation/theme/**']`. Já existe desde a PR 1, antes mesmo do design system (PR 4) — scaffold correto.
- **`src/domain/shared/` e `src/presentation/theme/`**: pastas já criadas (vazias) — estrutura de diretórios da arquitetura em camadas presente desde o bootstrap.
- **`Result<T,E>` e UUID v7**: cobertos por `src/shared/result.test.ts` e `src/shared/id.test.ts`, 9 testes.

## Achados

Nenhum.

## Observação de ferramental

`Unknown option "passWithNoTests" with value true` — warning de configuração do Jest, não falha. Registrado aqui para referência; não abre achado por severidade insignificante (apenas warning, não afeta resultado).

**PR-01 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-bootstrap-projeto-expo/`. Três capabilities auditadas. Mapeamento de cada `Scenario` do spec para o teste/evidência que o cobre, feito por inspeção direta do repositório (agente `qa-expert` não estava disponível como subagent_type nesta sessão; auditoria conduzida diretamente seguindo a mesma metodologia — read-only).

### Capability: `fronteiras-de-camada`

| Cenário | Teste/evidência | Status |
|---|---|---|
| Verificação de conformidade passa | `scripts/verificar-fronteiras.sh` (grep sobre `src/domain/`), rodado via `npm run verificar:fronteiras` → exit 0 confirmado nesta sessão | coberto |
| Import proibido é rejeitado (domínio) | `eslint.config.js` — bloco `files: ['src/domain/**/*.{ts,tsx}']` com `no-restricted-imports` (react/expo/drizzle/@react-native) + script grep acima | coberto (config), **sem teste automatizado que force uma violação e verifique a rejeição** |
| Caso de uso depende da interface, não da implementação | `eslint.config.js` — `boundaries/dependencies`, policy `application` não permite `infrastructure` | coberto (config estática via lint), sem teste que simule a violação |
| Apresentação não acessa o banco | mesma regra `boundaries/dependencies`, policy `presentation` não lista `infrastructure` | coberto (config), sem teste que simule violação |
| Apresentação usa do domínio apenas tipos e formatadores | não há regra de lint nem teste que distinga "import de tipo" vs "import de função de escrita" dentro de `domain` — a policy de boundaries só permite/nega por camada, não por natureza do símbolo importado | **descoberto** |
| Hex em componente é erro | `eslint.config.js` — `no-restricted-syntax` (regraSemHexDeCor) aplicada a `src/**/*.{ts,tsx}` e `app/**/*.{ts,tsx}`, `ignores: ['src/presentation/theme/**']` | coberto (config), sem teste automatizado (ESLint `RuleTester`) que prove que a regra dispara |
| Hex nos tokens é permitido | mesmo bloco, `ignores` cobre `src/presentation/theme/**` | coberto (config) |
| Verificação agregada (comando único) | `npm run verificar` = `verificar:fronteiras && lint && typecheck`, script `package.json` | coberto |
| Violação bloqueia | decorre do `&&` no script — qualquer etapa com exit≠0 interrompe a cadeia; não há teste que force uma violação real e confirme o exit code agregado | coberto (por construção do shell), sem teste dedicado |

**Lacunas**: nenhum dos cenários de "rejeição" (import proibido, hex, dependência entre camadas) tem teste automatizado que *dispare* a violação e confirme a resposta — toda a cobertura é configuração estática (regras ESLint existem) mais o script de fronteiras já validado. O cenário "apresentação usa do domínio apenas tipos e formatadores" não tem nenhuma verificação, nem estática nem de teste.

### Capability: `primitivos-compartilhados`

| Cenário | Teste que cobre | Status |
|---|---|---|
| Formato correto (UUID v7, 36 chars, versão 7) | `src/shared/id.test.ts:4-11` (`toHaveLength(36)`, regex de formato, `id[14]).toBe('7')`) | coberto |
| Ordenável por tempo de criação | `src/shared/id.test.ts:19-23` (`anterior < posterior`) e `:25-29` (timestamp injetado codificado nos primeiros bits) | coberto |
| Gerável sem coordenação externa (offline, sem rede) | nenhum teste explícito — `src/shared/id.ts` usa só `globalThis.crypto.getRandomValues` (sem I/O de rede, por inspeção de código), mas não há teste que afirme "nenhuma chamada de rede" ou rode em ambiente sem rede | **descoberto** |
| Sucesso carrega o valor | `src/shared/result.test.ts:4-9` | coberto |
| Falha carrega o erro tipado | `src/shared/result.test.ts:11-16` | coberto |
| Discriminação obriga tratamento (erro de compilação sem narrowing) | nenhum teste de tipo (`@ts-expect-error`, `tsd`, `expectType`) encontrado em `src/shared/` — a garantia hoje é só a assinatura de `Result<T,E>` como union discriminada, verificada implicitamente pelo `tsc --noEmit` do `verificar`, mas não há um teste que prove que acessar `.valor` sem checar `.ok` é rejeitado pelo compilador | **descoberto** |

### Capability: `projeto-base`

| Cenário | Evidência | Status |
|---|---|---|
| App inicia no development build | não há teste automatizado (E2E/smoke) — depende de F4/F5 (bloqueadas, requerem development build instalado) | **descoberto (bloqueado por F4)** |
| Rotas em arquivos, nunca em navegador manual | estrutura `app/` com Expo Router confirmada (`app/compra`, `app/produto/[id]`, `app/(tabs)`) | coberto por config/estrutura |
| TypeScript estrito | `tsconfig.json:3` `"strict": true`; `npm run typecheck` roda `tsc --noEmit` | coberto |
| Perfil de desenvolvimento existe (EAS) | `eas.json` — perfil `development` com `developmentClient: true`, `distribution: internal` | coberto por config |
| Expo Go não é caminho suportado | `README.md:23` e `CLAUDE.md:13,25` instruem development build e explicitam que Expo Go não é suportado | coberto por config/documentação |
| Camadas existem no repositório | confirmado por `find`: `src/domain`, `src/ports`, `src/infrastructure/db`, `src/infrastructure/repositories`, `src/application`, `src/presentation`, `src/shared` todos presentes | coberto por estrutura |
| Ausência de pastas genéricas por tipo | busca por `components/`, `services/`, `models/` na raiz e em `src/` (fora de `presentation/`) não retornou nada | coberto |
| Teste de domínio roda isolado (sem emulador) | `jest.config.js` — projeto `domain`, `testEnvironment: 'node'`, sem preset Expo; `npm run test:domain` | coberto |
| Teste de integração usa preset do Expo | `jest.config.js` — projeto `app`, `preset: 'jest-expo'`, separado do projeto `domain`/`infra` | coberto |
| Comandos declarados | `package.json` scripts: `start`, `android`, `ios`, `test`, `test:domain`, `test:cov`, `typecheck`, `lint`, `verificar:fronteiras`, `verificar` | coberto |
| Documentação atualizada | `CLAUDE.md` seção "Comandos" lista os comandos reais (não "a confirmar") | coberto |

### Achados desta rodada

- Ver `qa/achados/ACHADO-008.md` (lacuna de teste automatizado para as regras de lint de fronteira/hex).
- Ver `qa/achados/ACHADO-009.md` (lacuna de teste de tipo para discriminação obrigatória de `Result`).
- "App inicia no development build" permanece bloqueado por F4 (mesma dependência já registrada no plano) — não abre achado novo, é a mesma bloqueante já rastreada.

## F4 — Smoke no emulador

Pré-requisito confirmado nesta rodada: emulador `expo-dev` (AVD) subido em modo headless (`emulator -avd expo-dev -no-window -gpu swiftshader_indirect`), `device_id` Maestro `emulator-5554`, app `com.triasoftware.repor` já instalado, Metro (`npx expo start --dev-client`) de pé com `adb reverse tcp:8081 tcp:8081`.

- **Flow**: `.maestro/smoke-launch.yaml` (piso mínimo — PR-01 é o bootstrap do projeto, não introduz capability/tela própria; flow dedicado seria trabalho de F5).
- **Resultado**: passou. 6 comandos executados, `launchApp` + `assertVisible` das 4 abas (Despensa, Lista, Resumo, Configurações) confirmados sem tela em branco/redbox.
- **Verificação de display**: `adb shell dumpsys activity activities` confirmou display único (`Display #0`) e `topResumedActivity=com.triasoftware.repor/.MainActivity` — screenshot validada como a tela correta.
- **Evidência**: `qa/por-pr/evidencias/PR-01/smoke-launch.png` (tela Despensa renderizada com itens de exemplo).

**F4 desbloqueada nesta rodada** — resolve a bloqueante "App inicia no development build" registrada na seção F3 acima (capability `projeto-base`); não abre achado novo.
