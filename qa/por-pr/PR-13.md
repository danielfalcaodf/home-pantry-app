---
pr: 13
branch: feat/ajuste-visual-telas-design-system
commit: f6cb2fb
estado: concluida
---

## Foco do checklist

Ícone por aba, chip "Faltando" somando crítico+emFalta, rótulos `Acabou`/`Faltando`, `GraficoBarras`, `'worklet'` em `esmaecer`/`molar` (regressão que derrubava o stepper).

## `npm run verificar` / `npm test` / `npm run test:cov`

Esta PR é o topo da cadeia — os números já foram capturados integralmente na **F1** (`qa/fases/F1-baseline-topo.md`), no mesmo commit `f6cb2fb`: `verificar` verde, 609/611 testes (2 falhas = [[ACHADO-001]]/[[ACHADO-002]], originadas na PR 11), cobertura de domínio 99,04%/94,92% branch. Não repetido aqui para não duplicar execução.

## Verificações específicas

- **Ícone por aba**: `app/(tabs)/_layout.tsx` — `tabBarIcon` usando `IconeSvg` com `path` próprio para cada uma das 4 abas (despensa, lista, resumo, configurações).
- **Chip "Faltando" somando crítico+emFalta**: `src/presentation/format/agrupar-despensa.ts:65` — `faltando: critico + emFalta`, com comentário explícito de design ("'faltando' é um filtro só de apresentação... que soma os estados 'critico' e 'emFalta'"); testado em `agrupar-despensa.test.ts:11` ("soma crítico e em falta no total de 'faltando'") e `:40` ("'faltando' casa com crítico e em falta, mas não com cheio").
- **Rótulos `Acabou`/`Faltando`**: `app/(tabs)/index.tsx:39-40` — `{ valor: 'critico', rotulo: 'Acabou' }`, `{ valor: 'faltando', rotulo: 'Faltando' }` — vocabulário do usuário, não termo de sistema.
- **`GraficoBarras`**: `src/presentation/components/grafico-barras.tsx` existe com `grafico-barras.test.tsx` correspondente.
- **`'worklet'` em `esmaecer`/`molar`**: `src/presentation/theme/movimento.ts:24,29` — diretiva `'worklet'` presente em ambas as funções, com comentário explicando a razão ("ambas são chamadas de dentro de callbacks de animação que já..."). Confirma que a regressão que derrubava o stepper (mencionada no plano-fonte e no commit `23e9d0c` do histórico recente) está corrigida no topo.

## Achados relacionados fechados nesta PR

- [[ACHADO-006]] (crítico, origem PR 5) — permanece corrigido no topo.
- [[ACHADO-007]] (crítico, origem PR 12) — **corrigido aqui**: `react-native-get-random-values` declarado em `package.json:43` pelo commit `4121ccd` ("feat(icones): adiciona react-native-svg e componente IconeSvg"), parte desta PR.

## Achados

Nenhum novo — herda [[ACHADO-001]] e [[ACHADO-002]] (fuso horário, origem PR 11, ainda abertos).

**PR-13 concluída. F2 (cadeia incremental completa, PRs 1-13) finalizada.**

## F3 — Conformidade com specs

Change OpenSpec: `2026-08-06-ajuste-visual-telas-design-system`. 2 capabilities modificadas (`chrome-de-navegacao`, `tela-despensa`) + itens de `proposal.md` sem delta de spec formal. ~15 cenários mapeados.

### `chrome-de-navegacao` (modificado — 10 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Tab bar com 4 rótulos + ícone cada, sem MissingIcon | implementação `(tabs)/_layout.tsx:19-48`; `icone-svg.test.tsx:21-26` cobre o componente isolado, não a tab bar integrada — **ACHADO-051** | Parcial |
| Cor do ícone segue a aba ativa | implementação `(tabs)/_layout.tsx:15-16`; `icone-svg.test.tsx:35-53` testa `IconeSvg` isolado, não a troca de aba — **ACHADO-051** | Parcial |
| Ícone SVG por path do design system, Configurações com ícone próprio | `icones.ts:8-19`; `icone-svg.test.tsx:21-26` | Coberto |
| Voltar da Conferência de estoque | `conferencia.tsx:9,33`; sem teste de tela — **ACHADO-049** | Gap |
| Voltar do Diagnóstico → Configurações | `diagnostico.tsx:6,23`; sem teste de destino — **ACHADO-049** | Gap |
| Voltar da Lista básica → Despensa | `lista-base.tsx:9,45`; sem teste de destino — **ACHADO-049** | Gap |
| Voltar do Histórico de compras (lista/detalhe) | `compra/historico.tsx:7,61`; `compra/historico/[id].tsx:8,77`; sem teste de destino — **ACHADO-049** | Gap |
| Voltar do Histórico do produto → Detalhe do produto | `produto/[id]/historico.tsx:7,75`; sem teste de destino — **ACHADO-049** | Gap |
| Aba Configurações também desenha o botão | `(tabs)/configuracoes.tsx:9,62`; sem teste — **ACHADO-049** | Gap |
| Alvo de toque + rótulo acessível do `BotaoVoltar` (herdado) | `botao-voltar.test.tsx` cobre rótulo e `router.back()`, não dimensão — já em **ACHADO-048** (PR-12) | Parcial (achado existente) |

### `tela-despensa` (modificado — 5 cenários)

| Cenário | Teste | Status |
|---|---|---|
| Chips `Tudo`/`Acabou`/`Faltando`, sem `Cheio` dedicado | implementação `(tabs)/index.tsx:37-41,195-204`; lógica `agrupar-despensa.ts`; tela sem teste próprio | Parcial |
| Contagens corretas (Acabou=3, Faltando=15) | `agrupar-despensa.test.ts:11` | Coberto |
| Filtro "Acabou" aplicado | `agrupar-despensa.test.ts:46` (via `casaComFiltro`) | Coberto |
| Filtro "Faltando" aplicado (crítico+emFalta, `ok` some) | `agrupar-despensa.test.ts:40` | Coberto |
| Contagem atualiza sem recarregar ao mudar quantidade | implementação `(tabs)/index.tsx:87` (`useMemo`); sem teste de reatividade — **ACHADO-050** | Gap |
| Chip sem itens mostra zero e continua acionável | contagem zero testada (`agrupar-despensa.test.ts:26`); acionabilidade (`disabled`) sem teste — **ACHADO-050** | Parcial |

### Itens sem spec formal (proposal.md)

| Item | Teste | Status |
|---|---|---|
| Cabeçalho da Despensa com botões de ícone (buscar/adicionar), sem link "Conferência" fixo | implementação `(tabs)/index.tsx:148-182`; sem teste | Gap (sem spec formal) |
| `RodapeCompra` movido para acima do botão "Fechar compra" | implementação `compra/[id].tsx:114-127`; sem teste — **ACHADO-052** | Gap |
| Resumo: "Gasto este mês" como métrica `data.xl` | implementação `resumo.tsx:115-120`; sem teste — **ACHADO-052** | Gap |
| Resumo: gráfico de barras dos últimos 4 meses | `GraficoBarras` coberto isoladamente (`grafico-barras.test.tsx`); integração "últimos 4 meses" sem teste — **ACHADO-052** | Parcial |

### Achados novos desta rodada

| Achado | Severidade | Resumo |
|---|---|---|
| ACHADO-049 | média | Botão de voltar estendido a 7 telas novas, sem teste de destino específico de navegação em nenhuma |
| ACHADO-050 | média | Reatividade da contagem dos chips e acionabilidade do chip zerado sem teste |
| ACHADO-051 | baixa | Integração da tab bar (4 ícones + cor por aba ativa) sem teste |
| ACHADO-052 | baixa | Posição do `RodapeCompra`, métrica `data.xl` do gasto mensal e corte "últimos 4 meses" do gráfico sem teste de integração |

**Cobertura**: lógica de filtro/contagem (`agrupar-despensa.ts`) muito bem coberta; padrão sistemático de telas sem teste, consistente com todas as PRs anteriores desde a PR-05, se confirma pela última vez nesta rodada final — inclui agora navegação (destino do botão voltar) como uma variante nova desse mesmo padrão. Nenhuma divergência de implementação real encontrada (diferente da PR-08); todos os gaps são ausência de teste, não comportamento incorreto.

**F3 completa — 13/13 PRs auditadas.**
