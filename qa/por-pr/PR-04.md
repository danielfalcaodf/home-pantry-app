---
pr: 4
branch: change/design-system-tema
commit: 3d6cde3
estado: concluida
---

## Foco do checklist

Paridade dos dois temas e contraste (`tokens.test.ts`), escala tipográfica, `raio 0` na linha, alvo 48×48, toast que não empilha.

## `npm run verificar`

Fronteiras e typecheck limpos. Mesmo warning de lint já visto na PR 3 ([[ACHADO-005]], `Milesimos` não usado) — carregado, não é novo aqui.

## `npm test`

```
Test Suites: 21 passed, 21 total
Tests:       234 passed, 234 total
Time:        3.433 s
```

## Verificações específicas

- **Paridade dos dois temas**: `tokens.test.ts:17` — "expõem exatamente o mesmo conjunto de chaves" (Despensa/Porcelana).
- **`fillOpacity` correto por tema**: `tokens.test.ts:23` — "opacidade de preenchimento é 0,12 no escuro e 0,10 no claro", batendo com o CLAUDE.md.
- **Contraste AA**: `tokens.test.ts:61,67` — `action.azulejo` sobre `bg.base` e `text.onAction` sobre `action.azulejo` passam AA de texto grande.
- **Escala tipográfica**: `tipografia.test.ts:4` — nenhum tamanho excede 34pt; `:20` papel de dado é monoespaçado e tabular (IBM Plex Mono); `:34` referencia só pesos exportados pelos pacotes `@expo-google-fonts/*` (regra do CLAUDE.md sobre confirmar pesos antes de fixar constantes).
- **Alvo de toque 48×48**: `componentes-base.test.tsx:45-50` — `Botao` tem `minHeight`/`minWidth` de 48.
- **Estado nunca só por cor**: `componentes-base.test.tsx:88,93` — `ChipEstado` anuncia rótulo + contagem juntos, recebe cor pronta sem conhecer domínio.

## Fora de escopo desta PR (não é lacuna, é sequência esperada)

- **`raio 0` da linha de item / medidor "linha d'água"**: ainda não existe componente `ItemDespensa`/medidor nesta PR — só os ~7 componentes-base (Texto, Botao, CampoTexto, ChipEstado, EstadoVazio, TelaErro). Verificar a partir da PR 5 (despensa e cadastro).
- **`toast.tsx` existe no código desta PR, mas sem teste** (`toast.test.tsx` não encontrado). Já é um dos 6 componentes sem teste catalogados no plano-fonte (seção F3) — não abro achado duplicado aqui; será formalizado na auditoria de conformidade F3, com esta PR como origem.

## Achados

Nenhum novo (warning de lint já registrado em [[ACHADO-005]] desde a PR 3).

**PR-04 concluída.**
