---
pr: 4
branch: change/design-system-tema
commit: 3d6cde3
estado: concluida
---

## Foco do checklist

Paridade dos dois temas e contraste (`tokens.test.ts`), escala tipográfica, `raio 0` na linha, alvo 48×48, toast que não empilha.

## `npm run verificar`

Fronteiras e typecheck limpos. Mesmo warning de lint já visto na PR 3 ([[ACHADO-005]], `Milesimos` não usado) — carregado, não é novo aqui.

## `npm test`

```
Test Suites: 21 passed, 21 total
Tests:       234 passed, 234 total
Time:        3.433 s
```

## Verificações específicas

- **Paridade dos dois temas**: `tokens.test.ts:17` — "expõem exatamente o mesmo conjunto de chaves" (Despensa/Porcelana).
- **`fillOpacity` correto por tema**: `tokens.test.ts:23` — "opacidade de preenchimento é 0,12 no escuro e 0,10 no claro", batendo com o CLAUDE.md.
- **Contraste AA**: `tokens.test.ts:61,67` — `action.azulejo` sobre `bg.base` e `text.onAction` sobre `action.azulejo` passam AA de texto grande.
- **Escala tipográfica**: `tipografia.test.ts:4` — nenhum tamanho excede 34pt; `:20` papel de dado é monoespaçado e tabular (IBM Plex Mono); `:34` referencia só pesos exportados pelos pacotes `@expo-google-fonts/*` (regra do CLAUDE.md sobre confirmar pesos antes de fixar constantes).
- **Alvo de toque 48×48**: `componentes-base.test.tsx:45-50` — `Botao` tem `minHeight`/`minWidth` de 48.
- **Estado nunca só por cor**: `componentes-base.test.tsx:88,93` — `ChipEstado` anuncia rótulo + contagem juntos, recebe cor pronta sem conhecer domínio.

## Fora de escopo desta PR (não é lacuna, é sequência esperada)

- **`raio 0` da linha de item / medidor "linha d'água"**: ainda não existe componente `ItemDespensa`/medidor nesta PR — só os ~7 componentes-base (Texto, Botao, CampoTexto, ChipEstado, EstadoVazio, TelaErro). Verificar a partir da PR 5 (despensa e cadastro).
- **`toast.tsx` existe no código desta PR, mas sem teste** (`toast.test.tsx` não encontrado). Já é um dos 6 componentes sem teste catalogados no plano-fonte (seção F3) — não abro achado duplicado aqui; será formalizado na auditoria de conformidade F3, com esta PR como origem.

## Achados

Nenhum novo (warning de lint já registrado em [[ACHADO-005]] desde a PR 3).

## F4 — Smoke no emulador

Ambiente reaproveitado das rodadas anteriores: emulador `expo-dev` (`device_id` `emulator-5554`) e Metro já ativos.

- **Flow**: `.maestro/smoke-launch.yaml` (piso mínimo) + verificação adicional inline navegando até a aba Configurações (não existe flow dedicado para esta PR ainda — trabalho de F5).
- **Resultado**: passou. App abre sem redbox; tela de Configurações renderiza o seletor de tema (Automático/Claro/Escuro) com os tokens do design system aplicados — evidência visual de que a paridade dos temas e os componentes-base (`Botao`, chips de seleção) estão de pé em runtime, não só nos testes unitários de `tokens.test.ts`.
- **Verificação de display**: `topResumedActivity=com.triasoftware.repor/.MainActivity`, display único.
- **Evidência**: `qa/por-pr/evidencias/PR-04/smoke-configuracoes.png`.

**Ressalva**: teste rodou sobre o estado acumulado da branch `qa/plano-de-testes` (todas as telas até a PR 13 presentes), não um checkout isolado do commit `3d6cde3`/`change/design-system-tema`. A tela de Configurações vista aqui já inclui elementos de PRs posteriores (backup/restore, diagnóstico) que não existiam nesta PR isoladamente — a evidência visual de tema/componentes-base ainda é válida, mas o restante da tela não deve ser atribuído a esta PR.

**PR-04 concluída.**

## F3 — Conformidade com specs

Change OpenSpec: `openspec/changes/archive/2026-08-02-design-system-tema/`. Quatro capabilities auditadas (~40 cenários), incluindo um delta de `banco-local` (tabela de configuração). `qa-expert` seguiu indisponível como `subagent_type`; auditoria conduzida diretamente.

### Capability: `banco-local` (delta — tabela de configuração)

| Cenário | Teste | Status |
|---|---|---|
| Migration nova, não edição da inicial | `src/infrastructure/db/migrations/0001_configuracao.sql` é arquivo separado de `0000_init.sql` | coberto (estrutural) |
| Leitura de preferência ausente | `sqlite-configuracao.repository.test.ts:13` | coberto |
| Gravação sobrescreve | `sqlite-configuracao.repository.test.ts:24` ("gravação repetida substitui sem duplicar linha") | coberto |
| Configuração pertence à casa | todos os métodos do teste recebem `casaId` (linhas 13,18,24) | coberto |

### Capability: `componentes-base`

| Cenário | Teste/evidência | Status |
|---|---|---|
| Papel válido resolve a família correta | `componentes-base.test.tsx:26,33` | coberto |
| Papel inválido é rejeitado (tipo) | nenhum teste `@ts-expect-error`; mesma classe de lacuna do ACHADO-009/010 | **descoberto** |
| Números não deslocam o layout | `componentes-base.test.tsx:33` confirma `fontVariant: ['tabular-nums']` — mecanismo que garante largura fixa por dígito | coberto (indireto, mecanismo correto) |
| Alvo mínimo respeitado (botão) | `componentes-base.test.tsx:45` | coberto |
| Estado desabilitado perceptível sem cor | `componentes-base.test.tsx:53` | coberto |
| Rótulo permanece durante digitação | `componentes-base.test.tsx:64` | coberto |
| Foco é indicado (divisor 2pt) | `componentes-base.test.tsx:70` | coberto |
| Erro é descrito em texto | `componentes-base.test.tsx:81` | coberto |
| **Chip ativo é distinguível** (fundo na cor do estado com opacidade reduzida, texto na cor cheia) | `chip-estado.tsx:31` usa `backgroundColor: ativo ? tema.bg.raised : 'transparent'` — cor **neutra elevada**, não a cor do estado (`cor` prop) com `fillOpacity`. Nenhum teste exercita a prop `ativo`. | **descoberto — possível divergência de implementação, não só de teste** |
| Contagem faz parte do rótulo acessível | `componentes-base.test.tsx:88` | coberto |
| Toast: substituição em vez de empilhamento | `toast.test.tsx` não existe | **descoberto** |
| Toast: não bloqueia interação | idem | **descoberto** |
| Toast: tempo restante visível | idem | **descoberto** |
| Estado vazio oferece caminho | `componentes-base.test.tsx:100` | coberto |
| Estado vazio: vocabulário de interface (sem termos de sistema) | nenhuma asserção explícita contra "dar baixa"/"movimento de estoque"/"reposição" nos textos de `EstadoVazio` | **descoberto (baixa prioridade — mesmo padrão já testado no domínio)** |
| Tela de erro oferece ação | `componentes-base.test.tsx:112` | coberto |
| Falha de migration usa a tela de erro | mesmo cenário do [[ACHADO-012]] (PR-03) — não duplicado aqui | descoberto (já rastreado) |
| Redução de movimento: esmaecimento curto sem mola | `movimento.ts` declara `esmaecer()` com `withTiming`/`ReduceMotion.System`, mas nenhum teste renderiza um componente animado com `reduceMotion` ligado e confirma o comportamento | **descoberto** |
| Retorno tátil preservado com reduceMotion | nenhum teste (retorno tátil também não é avaliável em Jest/emulador — mesma ressalva de F6) | **descoberto (parcialmente inerente ao ambiente de teste)** |
| Preferência lida pelo sistema, não checagem manual | `movimento.ts:10` usa `ReduceMotion.System` diretamente, sem `if` manual — por inspeção de código, atende ao requisito | coberto (estrutural) |

### Capability: `tema-e-tokens`

| Cenário | Teste | Status |
|---|---|---|
| Mesma forma nos dois temas | `tokens.test.ts:17` | coberto |
| Opacidade de preenchimento distinta por tema | `tokens.test.ts:23` | coberto |
| Contraste mínimo AA | `tokens.test.ts:61,67` | coberto |
| Tema tipado (compilador rejeita token inexistente) | nenhum teste de tipo; mesma classe do ACHADO-009/010 | **descoberto** |
| Componente usa token, não literal | `componentes-base.test.tsx:120` (bloco "conformidade dos componentes-base": grep de hex em `src/presentation/components/`) — cobre os componentes-base especificamente | coberto |
| Literal em componente é erro de lint | `eslint.config.js` `no-restricted-syntax` (mesma regra auditada na PR-01); reforçado pelo teste de conformidade acima só para `presentation/components/` | coberto (config + teste parcial) |
| Espaçamento restrito à escala | `espaco.ts` declara escala fechada (4,8,12,16,24,32,48), mas **não há lint rule nem teste** que impeça um componente de usar um número arbitrário de espaçamento — ao contrário de cor, que tem `no-restricted-syntax` dedicado | **descoberto** |
| Raio zero na linha da lista | `item-despensa.tsx:62` usa `raio.linha` (=0); nenhum teste em `medidor-e-item.test.tsx` afirma `borderRadius === 0` explicitamente | coberto (config, sem teste dedicado — baixa prioridade) |
| Papéis tipográficos fixos (número → monoespaçada) | `tipografia.test.ts:20,27` | coberto |
| Limite de tamanho (34pt) | `tipografia.test.ts:4` | coberto |
| Padrão é automático | `resolver.test.ts:4` (lógica pura) | coberto |
| Escolha sobrevive ao fechamento | `usePreferenciaDeTemaPersistida` (`src/application/tema/use-preferencia-de-tema.ts`) implementa leitura/gravação via repositório injetável, mas **não existe `use-preferencia-de-tema.test.ts`** | **descoberto** |
| Automático acompanha mudança do sistema | mesma lacuna acima — hook não testado (só a função pura `resolverTema` é testada) | **descoberto** |
| Abertura em tema escuro não pisca branco | `app/_layout.tsx` gate splash até `bancoPronto && tema.carregada`; sem teste RTL, mais adequado a F6 (visual) | descoberto (dependente de F4/F6) |
| Splash só some após tema resolvido | mesma evidência de `_layout.tsx`; sem teste dedicado | descoberto (dependente de F4/F6) |

### Capability: `tipografia-carregada`

| Cenário | Teste | Status |
|---|---|---|
| Número usa família monoespaçada | `tipografia.test.ts:20` | coberto |
| Display restrito a título/detalhe | família de display foi removida do projeto (`fontes.ts` comentário: "Archivo (display) foi removida por orçamento") — cenário N/A no estado atual, nenhum papel usa família de display | coberto (por ausência deliberada, documentada) |
| Fontes carregadas antes do 1º quadro | `app/_layout.tsx` usa `useFonts` + gate de splash; sem teste, dependente de F6 | descoberto (dependente de F4/F6) |
| Pesos verificados antes de fixar constantes | `tipografia.test.ts:34` ("referencia apenas os pesos que os pacotes exportam") | coberto |
| Peso não usado não é carregado | `fontes.ts` só importa os 4 pesos referenciados por `tipografia.ts`; sem teste automatizado que compare as duas listas programaticamente (é verdade por inspeção manual, não gerado/verificado) | coberto (por inspeção, sem teste que preveniria regressão) |
| Orçamento de bundle medido (400kb) | `fontes.ts:9` comenta "696 KB embarcados" — **acima dos 400kb citados no spec como gatilho de remoção da família de display**, mesmo já tendo removido Archivo. Nenhum teste mede ou registra o peso automaticamente; o número vem só de comentário manual | **descoberto — divergência potencial (696KB > 400KB mesmo após remoção), revisão humana recomendada** |
| Nome cresce sem truncar (200%) | requer emulador/RTL com `PixelRatio` mockado; sem teste, dependente de F6 | descoberto (dependente de F4/F6) |
| Alvos de toque preservados na escala máxima | idem | descoberto (dependente de F4/F6) |

### Achados desta rodada

- Ver `qa/achados/ACHADO-015.md` — testes de tipo ausentes (papel inválido, tema tipado), mesma classe do ACHADO-009/010.
- Ver `qa/achados/ACHADO-016.md` — `toast.tsx` sem nenhum teste (formalização anunciada na própria PR-04.md).
- Ver `qa/achados/ACHADO-017.md` — `ChipEstado` com `ativo=true` usa cor neutra em vez da cor do estado com opacidade reduzida; possível divergência de implementação, não só de teste.
- Ver `qa/achados/ACHADO-018.md` — `usePreferenciaDeTemaPersistida` sem teste, apesar de ter repositório injetável pronto para fake.
- Ver `qa/achados/ACHADO-019.md` — comportamento de `reduceMotion` (esmaecimento curto, retorno tátil preservado) sem teste.
- Ver `qa/achados/ACHADO-020.md` — escala de espaçamento sem lint rule de enforcement (ao contrário de cor).
- Ver `qa/achados/ACHADO-021.md` — orçamento de bundle de fontes (696KB, comentário manual) acima do limite de 400KB citado no spec, sem medição automatizada.

**Resumo**: ~26/40 cenários cobertos; 14 descobertos (7 novos achados + 2 já rastreados/dependentes de F4-F6 recorrentes).
